# 用 Google Sheet 當資料庫（設定步驟）

大約 5 分鐘。做完之後寶可夢箱就存在你自己的 Google 帳號裡，跨裝置同步，跟 Claude 無關。

## 1. 建 Sheet

到 [sheets.new](https://sheets.new) 建一份空白試算表，隨便取個名字（例如 `pokemon-sleep-box`）。

不用手動建工作表 —— 腳本第一次寫入時會自動建 `_raw` 和 `roster` 兩個。

## 2. 貼上腳本

在那份 Sheet 裡：**擴充功能 → Apps Script**。

把預設的 `myFunction` 全部刪掉，貼上 `apps-script/Code.gs` 的內容。

## 3. 改金鑰

找到第一行：

```js
const TOKEN = 'CHANGE_ME_TO_A_LONG_RANDOM_STRING';
```

換成一串長一點的隨機字串。可以在終端機跑：

```bash
openssl rand -hex 24
```

**記下這串**，第 6 步要用。

存檔（Ctrl/Cmd + S）。

## 4. 先自我測試

編輯器上方的函式下拉選單選 `selfTest`，按**執行**。

第一次會跳授權：**審查權限 → 選你的帳號 → 進階 → 前往「未經驗證」→ 允許**。

（會出現「未經驗證的應用程式」警告，因為這是你自己寫的腳本、沒有送 Google 審核。這是正常的。）

執行完回去看 Sheet，應該多了 `_raw` 和 `roster` 兩個工作表。

## 5. 部署成 Web App

**部署 → 新增部署作業 → 齒輪圖示選「網頁應用程式」**，然後：

| 欄位 | 選什麼 |
|---|---|
| 執行身分 | **我**（`我的帳號`） |
| 誰可以存取 | **所有人** |

按**部署**，複製那個 **網頁應用程式網址**（長得像 `https://script.google.com/macros/s/AKfy.../exec`）。

> 「所有人」是必要的 —— 瀏覽器要能不登入就呼叫。存取控制靠的是第 3 步那串金鑰，所以**金鑰不要外流**。

## 6. 填進網頁

打開你的工具 → **寶可夢箱** 分頁 → 最上面「雲端同步」：

- **Apps Script Web App 網址**：第 5 步複製的
- **存取金鑰**：第 3 步那串

按 **連線並下載**。第一次雲端是空的，會提示你按「立即上傳」把本機資料推上去。

之後每次改動都會自動上傳（防抖 0.9 秒）。右上角會顯示「已同步 Sheet」。

網址和金鑰只存在**那台瀏覽器的 localStorage**，不會上傳。換裝置要重新填一次。

## 7. 驗收

回去開 Sheet，`roster` 工作表應該有一行一隻寶可夢的可讀表格（中文名、等級、性格、副技能、食材）。

`_raw` 是真的資料來源（JSON）。`roster` 只是給你看的鏡像 —— **在上面手改不會回寫**，要改資料請在網頁上改。

## 改版之後

改了 `Code.gs` 要重新部署才會生效：**部署 → 管理部署作業 → 鉛筆 → 版本選「新版本」→ 部署**。網址不變。

## 疑難排解

| 症狀 | 原因 |
|---|---|
| `連線失敗：unauthorized` | 網頁的金鑰和 `Code.gs` 的 `TOKEN` 不一樣 |
| `連線失敗：HTTP 401` / 跳登入頁 | 部署時「誰可以存取」沒選「所有人」 |
| `連線失敗：Failed to fetch` | 網址結尾不是 `/exec`（`/dev` 那個需要登入，不能用） |
| 改了 `Code.gs` 沒反應 | 忘記重新部署新版本 |
| 上傳成功但 Sheet 沒變 | 看 `_raw` 而不是 `roster`；或腳本綁到別份 Sheet 了 |
