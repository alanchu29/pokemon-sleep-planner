# 決策記錄

這個專案在一次 Claude Cowork 對話裡從零做出來（2026-09-07）。工具本身留下了，那段對話不會。這裡記的是**為什麼做成這樣**，以及**哪些數字是驗證過的、哪些不是**。

## 需求來源

使用者玩 Pokémon Sleep，每週要根據「本週島嶼加成樹果 + 本週料理主題 + 手上有的寶可夢」決定先發 5 隻。他要的不是查表，而是**窮舉最佳化**。

明確表達過的偏好：

- **中文介面** —— 他玩的是繁中版，英文對照很痛苦
- **不依賴 Claude 儲存資料** —— 要自架、要資料在自己手上（→ GitHub Pages + Google Sheet）
- **傾向按鈕／隨時要求，而非頻繁自動排程**
- 打字比下拉選單慢（中文輸入法要選字），所以 UI 以**下拉選單 + 打字跳選**為主，沒做文字批次匯入

## 關鍵設計決策

### 為什麼是窮舉，不是啟發式

C(30,5) = 142,506、C(40,5) = 658,008。把每位成員的模擬結果**記憶化**（key 為 `member × teamContext`）之後，每組隊伍的評分只是幾十次加法。26 隻實測 569 ms。啟發式沒有必要，而且會讓人不信任結果。

超過約 120 萬組合（≈43 隻）才會預篩到前 42 隻。

### 為什麼隊伍和食譜要一起最佳化

第一版是「先選食譜 → 照它挑隊伍」。這是錯的 —— 選錯食譜就得到為錯目標最佳化的隊伍。

改成：**對每組隊伍試遍所有候選食譜**，取最佳搭配再排名。實測（8 隻假資料、同一組隊伍）放開料理類型限制後，料理能量從 133k → 277k，**週總能量多 23%**。這種事看清單看不出來。

### 為什麼有兩階段（搜尋 → 決賽）

完整的多起點 21 餐排程對每組隊伍跑太慢（33,649 組要 4.4 秒）。所以：

- **搜尋階段**用 `mealPlan` 單起點 —— 貪婪、**會扣除食材**，是真值的下界
- **決賽 50 組**跑 `bestPlan` —— 多重起點（`null` ＋ `rankSingle` 前 8），繞過貪婪的非單調性

`FINALISTS = 50` 是踩過坑之後定的：原本 8 組，導致真正最佳的隊伍被擠出名單，**料理能量低估 27%**。

> ⚠️ **搜尋階段原本用的是 `proxyDish`（樂觀上界），那是錯的，2026-09-09 已修正。**
> 見下方〈搜尋用的 `proxyDish` 是壞的評分函式〉。下面兩節（`FINALISTS = 50` 夠用、
> 預篩會丟掉最佳解）都是在 `proxyDish` 還在的時候做的實驗。

### 為什麼一份程式碼兩種後端

使用者要自架，但 claude.ai 上的版本也還在用（那邊有跨裝置同步、我每週一自動更新資料）。分岔兩份會立刻走鐘。所以 `!window.claude` 執行時判斷。

## 修過的 bug（都不是靠讀程式碼找到的）

| Bug | 影響 | 怎麼發現的 |
|---|---|---|
| `$` 的 TDZ | **整頁死掉** | Sheet 同步的往返測試 |
| 貪婪排程不單調 | 調高食譜等級，總分反而變低 | 對照實驗 |
| 搜尋／評分目標脫鉤 | 料理能量低估 27% | 對照實驗 |
| 幫手加速沒算樹果縮放 | 雷公／炎帝／水君被低估 **49%** | 逐項稽核程式碼 |
| 揮指／技能複製／十項全能算 0 | 這些寶可夢的主技能產出當成零 | 逐項稽核程式碼 |
| 負電無條件給能量 | 沒有正電夥伴時高估 | 逐項稽核程式碼 |
| 緞帶沒有輸入欄位 | 引擎有算但填不進去 | 逐項稽核程式碼 |
| 自架版本的文案說謊 | 按鈕聲稱 Claude 會處理，實際什麼都不會發生 | 看使用者的螢幕截圖 |

**教訓**：對照實驗（同一組輸入改一個變數）比讀程式碼有效。`tests/smoke.mjs` 就是把這些檢查固化下來。

## 驗證過的數字

以下經過交叉比對（RaenonX 資料表 + Neroli's Lab 原始碼 + namu wiki 三方一致）：

- **活力檔位**：`≥80 → ×0.45`，而且 **80 到 150 是同一格**，超過 80 不會更快。上次改動是 2024-07 的 v1.9.0，只動了低活力檔位。使用者原本以為「超過 100 會更快」—— 機制上是錯的，但**追高活力仍然正確**，因為那延長待在最快檔位的時間（起床 100 撐 3.3 小時，起床 150 撐 11.7 小時）。
- **每日活力赤字**：衰減 −144／日，睡飽只回 +100（有活力回復獎勵 105）→ **淨 −44。光靠睡覺數學上不可能維持 80 以上**，這就是補師的存在意義。
- **v3.6.0（2026-06）後的上限**：等級 70、鍋子 81、地區加成 85%、副技能欄位 Lv10/25/50/70/80。**任何寫 75/100 解鎖、鍋子 57、加成 60% 的攻略站都是舊的。**

## `FINALISTS = 50` 驗證過了，但**預篩是壞的**（2026-09-07）

`FINALISTS = 50` 和「>120 萬組合就預篩到前 42 隻」這兩個捷徑，訂出來之後**從來沒被驗證**。`FINALISTS` 還有前科：原本是 8，導致料理能量低估 27%。所以做了對照實驗（`searchTeams` 的 `finalists` / `prescanLimit` 參數就是為此而留）。

### `FINALISTS = 50` 夠用 ✓

40 隻箱子（658,008 組），比較 `finalists` = 50 / 200 / 1000 → **前 8 名完全相同**，第 1 名 total 差 0.000%。

> ⚠️ **當時的結論「`proxyDish` 沒有把最佳隊伍擠出決賽」是錯的，而且這個實驗方法偵測不到那件事。**
> 放大決賽名額之所以不改變結果，正是因為決賽**也是按 `proxyDish` 的分數排序**：
> `finalizeTeams` 的 `if (mp.total > b.dishS)` 在上界之下幾乎永遠不成立，所以多放進來的
> 隊伍不會翻身。要驗的是「proxy 排名 vs 真實排程排名」，而那個對照 2026-09-09 才做 ——
> 結果是真實前 8 名**全部 8 組**擠不進 FINALISTS = 50（放到 1000 還有 5 組進不去）。
> 修正後重跑，`FINALISTS = 50` 才真的夠用（真實前 8 名 0 組落榜）。

### 預篩會丟掉最佳解，而且**結果隨箱子順序改變** ✗

預篩的「solo score」是 `scoreTeam([i] + pool.slice(0,4))` —— companions 是**按 roster 順序**取的任意 4 隻。而 roster 順序就是使用者把寶可夢加進箱子的順序，是個**完全不相關的輸入**。

60 隻箱子（預篩砍到 42，6.4x），同一批寶可夢、只改順序：

| 順序 | 預篩結果 | 相對全搜 |
|---|---|---|
| 原順序 | 1,522,202.82 | 0.00% |
| 洗牌 seed=7 | 1,522,202.82 | 0.00% |
| 洗牌 seed=99 | 1,522,202.82 | 0.00% |
| **洗牌 seed=4242** | **1,273,490.43** | **−16.34%** |

兩種順序的**全搜都找到同一組**最佳解（`CLEFABLE+GROTLE+RUFFLET+SALAMENCE+VAPOREON`, 1,522,202.82），所以最佳解是唯一的 —— 變數只有預篩。seed 4242 那次前 8 名與正解**零重疊**。

**這是和「搜尋／評分目標脫鉤」同一類的 bug**（那次是低估 27%），也同樣是靠對照實驗抓到的，不是讀程式碼。

**影響範圍**：只在觸發預篩時（>120 萬組合，約 43 隻以上）。箱子小於 43 隻完全不受影響。

**修法**：不要修那個啟發式 —— 多 worker 平行化之後**直接把預篩刪掉**，用窮舉取代。見 `TODO.md` 的「多 worker 平行化」。

**教訓（第二次了）**：先前我用三個箱子測過預篩、都通過，就寫下「驗證過了」。三次都剛好是好運的順序。**對這種依賴輸入順序的啟發式，測幾個案例通過不構成證據 —— 要嘛證明它與順序無關，要嘛把它拿掉。**

## 產品規則：樹果型必須產本週加成樹果（2026-09-07）

`wk.strictBerry`，**預設開啟**，UI 上是「樹果型只考慮本週加成樹果」那個勾選框。

### 這是需求，不是最佳化

使用者的立場：**樹果型寶可夢的職責就是產樹果**。不符合本週加成樹果的樹果型，本來就不在他的考慮範圍內；會入選代表工具的輸出不符需求。

我一開始把它當成「剪枝安不安全」在評估，那是誤解。它確實會讓總能量下降，但那個「更高的能量」對應的是使用者不想要的隊伍 —— 所以不是精度損失，是目標函數的修正。

### 實測的取捨（提出時已量化並確認）

40 隻箱子（13 隻樹果型），四組不同的本週樹果：

| 本週樹果 | 剩下候選 | 搜尋空間 | 第 1 名 total | 前 8 重疊 |
|---|---|---|---|---|
| ORAN/PAMTRE/PECHA | 29/40 | 5.5x 小 | −5.69% | 0/8 |
| DURIN/BELUE/RAWST | 28/40 | 6.7x 小 | −3.91% | 0/8 |
| LEPPA/FIGY/SITRUS | 30/40 | 4.6x 小 | −5.69% | 0/8 |
| GREPA/CHERI/WIKI | 30/40 | 4.6x 小 | −6.61% | 0/8 |

**被排除的代表例：Xatu（天然鳥）。** 專長 `berry`、樹果 `MAGO`（四組設定全都不合），但主技能是 **Ingredient Magnet S（食材磁鐵）** —— 牠原本會因為「生產食材」而入選。開了這個規則就不會。

這說明 `specialty` 是分類標籤、不是價值來源：樹果型也可能因為主技能、幫助頻率、或隊伍綜效（幫助加成、正電／負電）而有價值。**規則刻意忽略這些**，因為使用者要的是「樹果型就該符合樹果要求」。

### 三個實作決定

1. **`wk.fav` 是空的（還沒設本週樹果）→ 規則不生效。** 否則會把所有樹果型都排除。
2. **📌 固定的成員不受此限。** 使用者的個別指定優先於通則。
3. **排除名單要看得見。** `comboCount` 顯示「已排除 N 隻樹果不符的樹果型」，`title` 列出中文名。靜靜地少算候選是這個 repo 最不想要的行為 —— 那正是「文案說謊」那類 bug 的來源。

副作用：搜尋空間變小，實測 UI 上 197ms → 54ms（3.6x）。但那是附帶的，不是目的。

### 這和被移除的預篩不同

被移除的預篩是**壞的**（結果隨 roster 順序改變，見上一節）。這個規則是箱子內容的函數，與順序無關 —— `tests/smoke.mjs` 第 4 節對開／關兩種模式都斷言了順序不變性。

## 多 worker 平行化：2x，而且瓶頸不在我們這邊（2026-09-07）

拿掉預篩之後大箱子變慢（60 隻窮舉 5,461,512 組要 45 秒），所以把列舉分片給多個 worker。

**做法**：`shardAssign` 把第一層的每個 `i0` 貪婪裝箱指派給分片；各 worker 回傳自己的前 `FINALISTS` 名（精簡成 `{idxs, score}`）；主執行緒合併、取全域前 N、`rehydrate` 還原、跑一次決賽。

**正確性**：全域前 N 名必然也在各自分片的前 N 名內，所以合併集合一定包含它們 —— 分片結果與單執行緒窮舉**完全相同，不是近似**。`tests/smoke.mjs` 第 10 節斷言兩條路徑逐欄位相同，並在 2/4/6/8/12/16 個 worker 下都驗過。

### 三個實測發現

**1. `i % N` 分片是錯的。** i0 越小子樹越大（i0 的子樹有 C(n−1−i0, 4) 組），所以 modulo 交錯讓 shard 0 拿到 123,971 組、shard 7 只有 48,476 組 —— 差 2.6 倍，而**最慢的分片決定總時間**。改成貪婪裝箱後組合數差距降到 1.01x，40 隻的加速從 2.4x → 3.2x。

**2. postMessage 的序列化曾是主要瓶頸。** 完整的 `scoreTeam` 結果帶著 `outs`（5 個成員輸出，各含 Float64Array）和 `ing`，一個分片 241 KB、8 個分片 1.9 MB。精簡成 `{idxs, score}` 之後 32 KB（少 87%），編排開銷從數百 ms 降到 **27~46ms**。

**3. 加速在 6 個 worker 飽和在約 2x，而且不是我們的問題。** 量測（Intel Core Ultra 5 135H、18 邏輯核心、40 隻 658,008 組）：

| workers | 1 | 2 | 4 | 6 | 8 | 16 |
|---|---|---|---|---|---|---|
| 耗時 | 4776ms | 3544ms | 2818ms | 2441ms | 2360ms | 2413ms |
| 加速 | 1.0x | 1.3x | 1.7x | 2.0x | 2.0x | 2.0x |

**關鍵診斷：各分片耗時只差 1.06~1.11x，浪費 3~7%。** 也就是說分配是平衡的 —— 飽和不是負載不均，是機器沒有更多空閒 CPU（量測時有 25 個使用者自己的 Chrome 程序、31% 背景負載）。

**所以動態工作竊取不值得做**：它能買到的上限就是那 3~7%。原本以為混合架構（P-core/E-core）會造成分片不均，實測否定了這個假設。

`MAX_WORKERS = 6`：飽和點，而且刻意留幾個核心給 UI 與使用者的其他程式。

## 寶可夢箱的個體評分：從「邊際貢獻」改成「三軸產能」（2026-09-08）

箱子需要一個「這隻好不好」的數字（**不影響推演**，純顯示）。做了兩版。

### 第一版：加進固定參考隊的邊際貢獻 —— 已移除

`v(x) = f(參考隊 ∪ {x}) − f(參考隊)`，直接用 `scoreTeam`。想法是一次解決三個問題：單隻算料理會壓低食材型、補師的價值不在自己身上、本週加成會讓分數週週差兩倍。

**問題是它對參考隊的組成太敏感。** 換三種參考隊實測同一批候選：

| 候選 | a 2食2其他 | b 全樹果＋技能 | c 1食2果1技 |
|---|---|---|---|
| 食材型 Lv60 | 18.31萬 | **9.75萬** | 14.20萬 |
| 樹果型 Lv60 | 20.36 | 21.54 | 21.54 |
| 補師 Lv60 | 13.44 | 12.49 | 11.57 |

**食材型差 1.9 倍。** 原因是 **21 餐是硬上限**：任何像樣的隊伍都填得滿 21 餐，所以額外的食材只有在「解鎖了更高價的食譜」時才加分。參考隊食材越多，再加食材越沒價值。樹果型幾乎不動（樹果能量線性可加）—— 也就是說**偏差只打在其中一種專長上**，那比全體都有偏差更糟。

同一次調查還釘死了一件事（值得留著）：Lv30 妙蛙花裝 `Ingredient Finder M` 的邊際值是 **−0.21萬**。不是 bug —— 食材機率 0.266→0.362 讓背包更快裝滿（`helpsTillFull` 16.2→14.2），在有補能量的隊伍情境下夜間幫忙被截斷 → `procs` 下降 → 食材磁鐵灑出來的**其他種類**食材全部變少，而那些才是高價食譜要的。食材總量上升（59.3→76.9/日）但組成變差。**引擎是對的，那隻該先補 `Inventory Up`。**

另外也試過把 `dishS` 從 `proxyDish` 換成決賽的 `bestPlan`：**每一格數字完全沒變**，只多花 9 倍時間。因為 `proxyDish` 是上界而 `bestPlan` 恆 ≤ 它，`finalizeTeams` 那個 `if (mp.total > b.dishS)` 在自動配對模式下幾乎不會成立。

> ⚠️ **這一段描述的現象是真的，但當時把它讀成「proxy 夠好」—— 那個結論是錯的。**
> 「換成 bestPlan 數字完全沒變」的原因正是 `if` 不成立，也就是**決賽根本沒在修正分數**。
> 那不是 proxy 準，是決賽失效。2026-09-09 修正，見〈搜尋用的 `proxyDish` 是壞的評分函式〉。

### 第二版：三軸產能（現行）

使用者指出根本問題：**三種專長的職責本來就不同，分數不該互相比較。** 樹果型看產量、食材型看產能（配不配食譜是推演的事）、技能型看發動頻率。

改成每個專長各有自己的主指標之後，**參考隊整個消失** —— `monPower` 只需要 `baseStats` + `memberOutput`，是純函式，不碰 `POOL`，也就沒有「參考隊選得好不好」這個問題。基準只剩「單獨一隻、`nHB = 0`」，而那正是遊戲寶可夢詳細頁的情境，數字對得起來。

代價（已標在 UI 上）：**團隊型副技能量不到**。「幫忙加成」的價值在加速四個隊友，單獨一隻只看得到自己那 5% —— 所以標一個「隊伍型」徽章，量不到就說量不到。

考慮過但沒做的替代方案：**箱內平均邊際貢獻**（在你自己的箱子裡隨機抽隊友取平均，Shapley 的精神，約 0.85 秒）與**機會成本**（推演時順手記每一隻所在的最佳隊伍分數，幾乎免費）。兩個都仍然成立，而且和三軸產能互補 —— 前者答「在我的箱子裡平均值多少」，後者答「這週牠有沒有用」。要做的話見 `TODO.md`。

## 主技能發動次數：夜間幫忙被算了兩次（2026-09-09）

使用者的質疑：「推薦出來的大部分都是技能型……我們要考慮實際操作收穫，醒著時間才有可能去發動技能。」

`simulate` 原本是：

```js
const productive = helpsDay + nightNormal;
const bankedProcs = bs.p.sp==='skill' ? 2 : 1;
const procs = productive*bs.effSkill + Math.min(bankedProcs, nightNormal*bs.effSkill);
```

**`bankedProcs` 這個上限的存在，本身就承認「睡眠期間的技能發動要被 cap」** —— 但
`productive` 已經把 `nightNormal` 完整乘過一次 `effSkill`（而且沒有上限），然後又加了
一次 banked。同一批夜間幫忙算了兩次。這行沒有留下任何決策記錄，比較像是寫的時候滑掉。

改成 `helpsDay*effSkill + min(bankedProcs, nightNormal*effSkill)`。

**關鍵在於偏差只打在其中一種專長上。** `productive` 含夜間對**樹果與食材是對的** ——
睡覺時撿的東西醒來會收到，所以 `berries` 和 `memberOutput` 的 `ing` 照樣用 `productive`。
於是這個重複計算等於單獨給技能型加了 20% 的分。實測（Lv55、睡 8.5h、pot120/食譜Lv55）：

| | 舊 | 新 | |
|---|---|---|---|
| AMPHAROS 技能能量/週 | 190,026 | 150,468 | **−20.8%** |
| DARKRAI | 254,114 | 201,779 | −20.6% |
| DRIFBLIM | 170,821 | 135,261 | −20.8% |
| 樹果型的樹果收入 | — | — | **不變** |
| 食材型的食材顆數 | — | — | **不變** |

陣容影響（23 隻均衡箱、pot57/食譜Lv20/全食譜）：第 1 名從「樹果4/食材1」1,693,711 變成
「樹果4/技能1」1,508,482，而且前五名開始出現「樹果2/**食材2**/技能1」——
食材型從幾乎不入選變成穩定有席次。

同時檢查掉的兩個嫌疑（**都不是原因**）：

- `effSkill` 的保底放大（`pity = skill ? floor(144000/f) : 78`）實際只有 **1.02~1.09x**
- `snack`（背包滿之後的幫忙）本來就不觸發技能，那部分是對的

考慮過但**沒**採用的更嚴格模型：夜間完全不發動技能（`procs = helpsDay*effSkill`）。
那會讓技能型再低約 15%、總分 −22%，食材型穩定佔 2 席。沒採用是因為 `bankedProcs`
描述的「睡眠期間累積、醒來結算」才是遊戲的實際機制（使用者確認）。

**這個改動也會動到寶可夢箱的個體產能** —— 技能型的主指標就是 `procs`。同專長內是
接近等比例的下降（banked 那一項不變，所以不完全等比例），跨專長本來就不可比。

## 自組隊伍分頁（2026-09-09）

需求：「我既需要單看我自己排出來的隊伍進行呈現……我知道目前已經有 pin 的功能，但這在 UI 上操作不好使用。」

**引擎層本來就支援單一隊伍評估** —— 📌 固定 5 隻時 `combinations` 的 `need === 0` 分支只列舉那一組。所以這個功能沒有新的計算，只有 UX：不用去箱子裡一隻一隻找、按 pin、算完再清掉。

設計問答（完整過程見這一版的對話）定下的：

| 決策 | 結論 | 為什麼 |
|---|---|---|
| 隊伍數 | 動態 N 支，上限 **4** | 不是效能問題（單隊 `scoreTeam` 是毫秒級），是比較超過 4 欄讀不動 |
| 持久化 | **不進 `serialize()`** | 純檢視。因此 `SCHEMA` 不用 +1，也沒有雲端同步問題 |
| 不足 5 隻 | **不給結果** | `teamContext` 的 `energyTeam*5` / `qE(energy/5)` 都寫死 5 人 |
| 選擇器 | 浮層 ＋ 搜尋 | `<select>` 在 60 隻的箱子裡會變成翻找；內嵌展開每次都把結果推走 |
| 比較基準 | 一律對**隊伍 1** | 對「正在看的那隊」算會讓數字隨點擊變動；對「最高分」算則在比較兩個非最佳方案時繞路 |
| `strictBerry` | 不擋選，但要提示 | 使用者：「排除是在推演不選，我手動權力最大」 |

**唯一有回歸風險的部分是 `renderResults` 的重構。** 它原本讀全域 `lastResults[shownAlt]`、寫死輸出到 `#results`，現在拆成 `teamDetailHTML(r, opts)`（5 個共用 panel）＋ `bindTeamDetail(host, after)` ＋ `renderResults()`（空狀態 ＋ 替代隊伍表）。

考慮過讓自組隊伍自己寫一份渲染器（不碰推演，零回歸風險），**沒有採用**：兩個分頁對同一支隊伍給出不同數字，比沒有這個功能更糟。這和 `monCard` 同時給箱子與截圖校對區用、`impInterval` 呼叫引擎的公式而不自己算是同一條原則。

驗證（引擎層，用 Cursor 內附的 node ＋ `vm` 跑）：

- 自組隊伍 vs 推演對同一組 5 隻：`total` / `berryS` / `dishS` / `skillS` / `cooksCapped` / `rv` / `potEff` **全部相同**，選中的主食譜也相同
- 自組隊伍 vs 「把那 5 隻 pin 起來跑 `searchTeams`」（組合數 = 1）：**1,508,482 完全相同**
- `members` 的排列順序不影響結果

`tests/smoke.mjs` 第 11j 節把這些搬進瀏覽器，另外測索引維護（`teamsAfterDelete` / `teamsReset`）、選擇器的同隊去重與跨隊重複、刪除的 confirm、上限、以及 `strictBerry` 的提示。

## 食材利用率低的原因不是鍋容量（2026-09-09，同一天就被抓到並修正）

加「食材利用率」提示時，第一版的文案是：

> 一週最多 21 餐、每餐最多 81 個食材，所以食材型再多也吃不下；要讓料理這一塊變高得先提高鍋子容量或食譜等級。

使用者當場看出不對：「21餐*81個為什麼是只煮掉 519?」—— `21 × 81 = 1701` 比產出的 1393 還大，鍋容量根本沒用滿。

實測確認診斷完全歸錯了原因：

| 隊伍 | 鍋容量上限 | 實際煮掉 | 21 餐 | 剩下什麼 |
|---|---|---|---|---|
| 全食材隊 potEff 81 | 21×81 = 1701 | 1373（28%） | **全滿** | 蜂蜜 1259、香腸 686、牛奶 667 |
| 樹果隊 potEff 317 | 6657 | 1123（49%） | 全滿 | 蘋果 506、馬鈴薯 214 |
| 樹果隊把鍋子加到 332 | 6972 | 992（**52%**） | 全滿 | 蘋果 500、番茄 145 |

**鍋子加到 4 倍，利用率一動也不動。** 真正的原因是木桶效應：`mealPlan` 的 `cooks = min(floor(pool[i]/a))`，每道料理要湊齊它需要的每一味。那支全食材隊剩 1259 個蜂蜜，卻因為可可與蛋各只有 104 個而煮不了 `FLOWER_GIFT_MACARONS`。

修正後的文案把兩件事分開判斷：「已煮的 21 餐用不掉你的食材」（看 `leftover`，列出剩最多的那幾味）與「更貴的食譜連進鍋的機會都沒有」（看 `POOL` 前段有沒有 `cnt > potEff`）。**只有後者加鍋容量才有用。** 混在一起的話輸出會同時說「不是鍋子太小」和「加鍋子才有用」，第 11j 節現在直接斷言那兩句不能同時出現。

順帶在「這隊最能煮的食譜」表加了「卡在」欄 —— `rankRecipesForTeam` 本來就算了瓶頸食材 `bn`，只是一直沒顯示出來。

**教訓：一個「診斷型」的提示如果把原因歸錯，比不提示更糟** —— 它會讓人去改一個沒用的設定，然後以為工具壞了。這類文案一定要拿真實數字驗證過每一個分支。

## 搜尋用的 `proxyDish` 是壞的評分函式（2026-09-09）

使用者問：「即使原定的高級食譜缺乏食材做不出來，我們依然可以做其他的食譜去作出一餐，這也有在你的推薦演算法之內嗎？」

答案是「決賽有，搜尋沒有」—— 而查證的過程翻出了一個從一開始就存在的 bug。

`proxyDish` 走訪 `POOL` 填滿 21 餐，但**不扣除食材**：同一批蘋果被每一道用到蘋果的食譜重複計算。它的註解寫著「Over-counts, but ranks teams the same way the real plan does — which is all the search needs」。**後半句是錯的。**

33,649 組窮舉對照（24 隻箱子、鍋 54＋券、食譜 Lv30、全食譜）：

| 評分函式 | 相對真值的比值（中位數／最高） | 選出的隊伍真實分數 | 真實前 8 名進不了 FINALISTS = 50 | 額外成本 |
|---|---|---|---|---|
| `proxyDish`（舊） | **1.389** / 2.85 | 比最佳**低 9.17%** | **8 組全部** | — |
| `mealPlan` 單起點（新） | 1.0000 / 1.0000 | **0.00%** | 0 組 | +466ms |
| `bestPlan` 多起點 | 1.0（定義） | 0.00% | 0 組 | +4373ms |

**關鍵不是「高估」，是「高估的幅度會變」。** 一致的高估倍率不影響排名；但 `proxyDish` 的高估幅度取決於隊伍的食材種類分布（種類越集中、重複計算越多），所以它連排序都不保。推薦的陣容因此完全不同：

```
proxy 選出  SALAMENCE, STEELIX, SWAMPERT, BLAZIKEN, KANGASKHAN   (4樹果1食材)
真實最佳    SWAMPERT, BLAZIKEN, KANGASKHAN, GALLADE, ESPEON      (2樹果1食材2技能)
```

預設設定（鍋 57／Lv20／只咖哩）下損失 **10.43%**，所以不是特定設定的巧合。

**第二個症狀（使用者畫面上看得到）：** `finalizeTeams` 的 `if (mp.total > b.dishS)` 因為上界永遠比較大而幾乎不成立 —— 決賽算了真實排程卻沒用它。於是 UI 的「料理」數字（proxy 值）和下面 21 餐排程表的小計（真值）**差 58%**。

### 修法

`scoreTeam` 的兩個分支都改用 `mealPlan`：

- **auto**：`max(bestSingleRecipe, mealPlan(單起點))` —— 兩個都是可達的下界
- **manual**：`mealPlan(forced = wk.recipe)` —— 以前只算指定那一道，但決賽的 `bestPlan(forced)` 會用別的食譜填滿剩下的餐，低估中位數 **26%**（比值 0.743），真實前 8 名有 5 組擠不進決賽。同一個病的另一面。

`mealPlan` 單起點恆 ≤ `bestPlan`（後者的 seeds 包含 `null`），所以它是**下界** —— `finalizeTeams` 那行 `if` 因此會真的生效。實測 **47.7%** 的隊伍多起點比單起點好，那些就是靠這行修正回來的。

`proxyDish` 整段移除，原地留註解記錄教訓（和 `PRESCAN_LIMIT` 同樣的處理）。

### 驗證

| 項目 | 結果 |
|---|---|
| `dishS` ＝ 21 餐排程表小計 | 三種設定**全部一致**（79,382 / 696,017 / 1,716,754） |
| 搜尋選出的 = 真實最佳 | 損失 **0.000%**（auto 與 manual 都是） |
| 搜尋分數是下界 | 比值最大 1.0000 |
| 真實前 8 名進不了決賽 | 0 組（FINALISTS = 50） |
| **單調性**（陷阱 3） | 3 種設定 × 40 組隨機等級 = 120 次，**零下降** |
| 順序不變性（第 4 節） | 正序／反序完全相同 |
| 效能 | 33,649 組單執行緒 550ms → 1400ms（**1.84x**） |

單調性特別要測，因為單起點貪婪本身**不是單調的**（陷阱 3）—— 但決賽的 `bestPlan` 多起點壓住了它，實測沒有反例。

### 教訓

**「便宜的近似只要排序對就好」是一個需要證明的宣稱，不是可以寫在註解裡的假設。** 這個 repo 的其他兩次同類錯誤（roster 順序預篩、用 `specialty` 猜價值）都是同一個形狀：為了省時間引入一個沒驗證過的排序代理。驗證方法也要對 —— 舊的「放大 FINALISTS 前 8 名不變」實驗**偵測不到**這個問題，因為決賽本身也是按 proxy 排序的。

## 鍋子的剩餘空位會被別的食材填滿（2026-09-09）

使用者：「即使食譜用不到鍋子上限，在這遊戲裡還是可以填充其他的食材填滿鍋子，把食材能量加進去做成料理，這部分你是這樣計算的嗎？」

**不是。整個料理計算漏了這個機制。**

`recipeValue` 只算食譜指定的食材，`mealPlan` 也只扣除食譜指定的量。所以一道只要 23 個食材的食譜，在容量 81 的鍋子裡就只用 23 個，剩下 58 格空著 —— 那些空格在遊戲裡是可以塞食材換能量的。

### 為什麼沒早點發現

資料結構誤導：`recipes[].cnt` 剛好等於 `ings` 的總和（`OVERGROW_AVOCADO_GRATIN` 是 22+20+41+32 = 115），看起來就像「食譜的食材數是固定的、鍋容量只是門檻」。上游（Neroli's Lab）的模型也是這樣，而快照裡沒有任何「填充」相關的欄位。

**這是「資料結構長什麼樣」和「遊戲怎麼運作」不一致的例子** —— 前者無法證明後者。這一輪之前我還根據那個結構寫了兩版錯誤的診斷文案（見上一節的表格）。

### 實作

填充放在**所有食譜排完之後**：

```js
let room = 0;
for (const x of plan) room += Math.max(0, potEff - x.r.cnt) * x.n;
// 剩餘食材依原始能量由高到低塞進 room 格
```

三個決定：

- **排完才填**：那樣填充不會搶走食譜需要的食材，排程仍由「單道能量最高」決定（食譜的 `rlb × bonus` 倍率約 2.4，遠高於食材原始能量）。結果可達，所以 `mealPlan` 仍是真值的下界 —— 陷阱 5 的不變量靠這件事。
- **能量高的先塞**：空位有限，同樣一格放值錢的。
- **額外食材只計原始基礎單價**：

  | | 食譜等級 `rlb` | 食譜加成 `bonus` | 大成功 | 島嶼加成 |
  |---|---|---|---|---|
  | 食譜規定的基礎食材 | ✓ | ✓ | ✓ | ✓ |
  | 額外塞進去填鍋的 | ✗ | ✗ | ✓ | ✓ |

  使用者的說明：「130 分的火辣香草丟進 Lv1 的蘋果汁或 Lv60 的馬卡龍，都一樣是 130。」但大成功與島嶼加成作用在**整鍋總和**上，所以那兩個照吃 —— 程式上就是 `fillE * mul`。

  ⚠ 中途一度誤讀成「也吃食譜等級與加成」而改過一版（料理分數會再高一倍、陣容變成 2 食材型），使用者當場更正，已還原。**這一欄的正確性完全依賴遊戲機制，程式碼與快照都無法自證。**

### 已知近似：貪婪沒把「留下多少空位」算進去

挑食譜是按 `rv`（單道能量）由高到低，但空位現在是有價值的 —— 食材數少的食譜留的空位多，理論上「rv 稍低但 cnt 小」可能反超。實測（591 組抽樣 × 3 種設定）：

| 設定 | 考慮空位會更好的隊伍 | 平均多 | 最多多 |
|---|---|---|---|
| 鍋54+券 / Lv30 / 全食譜 | 4.6% | 1.07% | 4.92% |
| 鍋57 / Lv20 / 只咖哩 | 2.4% | 0.26% | 0.75% |
| 鍋120 / Lv55 / 全食譜 | 4.1% | 1.71% | 5.74% |

影響存在但不大，而正確估「空位單價」得在每一輪對剩餘食材池排序（很貴）。暫時不做，記在 `TODO.md`。**不要把它讀成「貪婪是最佳的」—— 它不是。**

### 影響（實測）

| | 修正前 | 修正後 |
|---|---|---|
| 使用者那一週的料理分數 | 187k | **383k（+104%）** |
| 24 隻箱子的最佳隊伍 | SWAMPERT,BLAZIKEN,KANGASKHAN,GALLADE,ESPEON（2樹果1食材2技能） | SALAMENCE,STEELIX,BLAZIKEN,KANGASKHAN,GALLADE（**3樹果1食材1技能**） |
| 樹果隊的食材利用率 | 49~52%（會跳出警告） | 超過 75%（不再警告） |

料理分數翻倍讓樹果／食材型回到推薦名單 —— 那正是使用者從第一個問題就在質疑的方向。

### 驗證

| 項目 | 結果 |
|---|---|
| `dishS` ＝ 21 餐排程表小計（含填充列） | 三種設定全部一致 |
| 搜尋選出的 = 真實最佳 | 0.000%（auto 與 manual） |
| 搜尋分數仍是下界 | 比值最大 1.0000 |
| 真實前 8 名進不了決賽 | 0 組（FINALISTS = 50） |
| **單調性**（陷阱 3） | 3 設定 × 40 組 = 120 次，零下降 |
| 順序不變性 | 正序／反序相同 |
| 效能 | 1353ms / 33,649 組（填充是 O(NING) 的加法，幾乎免費） |

### 還沒做的

**空餐（`idleMeals`）仍然不計分。** 那是拌拌料理，遊戲裡有能量但快照沒有係數。UI 現在寫明「這個工具不計分」。

## 收取區間：白天也要分段（2026-09-09）

使用者一路在問「主技能是否太理想？實務上真的這麼優秀嗎？畢竟我可能不是這麼頻繁地去點擊觸發技能」，最後給出決定性的兩句：

> 這遊戲不是自動收取的，是你得上線點才會收取。
> 此外，技能型寶可夢最多可以累積兩次技能觸發在身上收取。

**模型只有夜間套了這兩個上限，白天完全不套** —— 等於假設整個白天隨時在收。

### 影響：只打在「技能觸發率高」的身上

| | 觸發率 | 隨時收 | 每 3h | 每 4h | 每 6h |
|---|---|---|---|---|---|
| 哥達鴨 | 20.4%（全 dex 第 2，中位數只有 3.9%） | 18.53 | 12.33 | **9.75（53%）** | 7.17 |
| 風速狗 | 7.6% | 10.69 | 10.69 | 9.05 | 6.65 |
| 達克萊伊 | 3.8% | 4.08 | 4.08 | 3.45 | 2.53 |

觸發率越高越容易撞到「每段最多 2 次」的天花板。**這是一個系統性的偏差，而且只偏袒其中一類** —— 和 6d（夜間 procs 算兩次）、陷阱 5（proxyDish 的高估幅度隨隊伍而變）是同一個形狀。

同一支隊伍的總能量：3h **−6.9%**、4h **−15.4%**、6h **−27.1%**。

### 反直覺：收得越不勤，食材型也吃虧

袋獸的持有上限只有 21（全 dex 最低那一批），6 小時就裝滿掉 26%，料理跟著掉。實測 `collectH=6` 時最佳陣容從「樹果3/食材1/技能1」**變回**「樹果1/技能4」—— 因為技能型雖然被 cap，食材型被 cap 得更兇。

樹果型反而最不受影響（持有上限大、掉落慢），第 1 節的隊伍樹果收入從 1,393,576 只掉到 1,383,491。

### 實作

`simulate` 把白天切成 `wakeH / wk.collectH` 段，和夜間共用同一個 `segment(h)`：

```js
const segment = (h) => {
  const normal = Math.min(h, helpsTillFull);
  return {normal, snack: Math.max(0, h - normal),
          procs: Math.min(normal * bs.effSkill, bankedProcs)};
};
```

- **`collectH` 沒有值就走「隨時收」的舊行為** —— 舊的雲端資料不會突然變一個數字出來
- **`SCORE_WK` 固定 `DEFAULT_COLLECT_H = 4`**，不跟著週設定跑（個體產能要跨週可比），但一定要有值
- UI 加了「白天多久收一次」欄位，預設 4 小時

### 驗證

| 項目 | 結果 |
|---|---|
| 哥達鴨 4h 收一次 | **9.75 次**（與手算的期望值逐位相同） |
| `dishS` = 21 餐排程小計 | OK |
| 順序不變性 | OK |
| **單調性**（30 組隨機食譜等級） | **零下降** |

## 排程開場全窮舉：做了、量了、退回（2026-09-09）

使用者：「我怕我們沒有好好估算所有組合配上所有料理的可能性」「我就是需要最佳解」。

**隊伍組合是完全窮舉的**（`combinations` 列舉 C(n,5) 每一組），但**每組隊伍配哪些食譜是貪婪的**。`bestPlan` 只試 9 個開場（`null` ＋ `rankSingle` 前 8），而 `rankSingle` 排的是「一直煮這道」的總分 —— 大菜常常只煮得出 1~2 次，在那個排序裡很後面，卻可能是最好的開場。

改成「每一道煮得出來的都當開場」（9 → 最多 78）確實更好，實測 120 組抽樣：

| 設定 | 比只試 8 個好 | 平均多 | 最多多 |
|---|---|---|---|
| 鍋54+券 / Lv30 / 全食譜 | 41.7% | 0.90% | 4.26% |
| 鍋57 / Lv20 / 只咖哩 | 10.0% | 0.15% | 0.56% |

**但單調性掛了**：40 組隨機食譜等級裡 5 組總分下降，最多 −1.405%。

### 三個修法方向，全部失敗

| 修法 | 單調性 | 代價 |
|---|---|---|
| 加大 `FINALISTS`（150 / 200 / 400 / 500 / 1000） | **5/40，−25,377** | 推演 1.2→2.8 秒 |
| 搜尋階段也試 3 個開場 | **5/40，−25,377** | 推演 19→37 秒 |
| 搜尋階段也試 8 個開場 | **5/40，−25,377** | 推演 19→**82 秒** |
| 貪婪比較加空位價值 | 4/40，−19,812 | 基準分數還降 0.3% |

**下降的組數與幅度分毫不差**，連 `FINALISTS = 1000` 都一樣。這排除了兩個假設：

1. **不是「最佳解擠不進決賽」** —— 對照「全 33,649 組都跑決賽排程」的真值（跑了 13 分鐘），`FINALISTS = 50` 就已經選中真值。
2. **不是搜尋與決賽的落差** —— 把搜尋也改成多起點（讓兩邊估計方式接近）完全沒改變任何一組。

**根因是 `bestPlan(全起點)` 本身對食譜等級不單調。** `mealPlan(seed)` 本來就不單調（陷阱 3），取更多 seed 的 max 不保證單調。9 個開場時 seeds 來自 `rankSingle`、**會跟著食譜等級一起變**，那個「自適應」似乎剛好補償了非單調性；全窮舉的 seeds 固定不變，反而失去補償。

### 決定：退回

單調性（調高食譜等級不該讓總分變低）是使用者看得見的保證，比 0.9% 重要。要再試的方向是**讓排程本身單調**（DP 或可證明的界），不是加開場數或放大決賽名單 —— 那兩條已經證實無效。記在 `TODO.md` 第 10c 項。

### 附帶發現：`collectH` 會改變單調性測試的靈敏度

同一個有問題的改動，`collectH ≥ 3` 時測出 0/40、`≤ 0.5` 時測出 5/40 —— 因為主技能被 cap 之後分數差距拉開，臨界情況被蓋住。**「在預設設定下測過了」不構成證據。**

## 沒有驗證過的

- **從來沒跑過真實資料**。所有測試都用捏造的寶可夢。使用者的箱子在交接時還是空的。→ `TODO.md` 的第 0 項就是要補這個，**它決定其他每一項該不該做**。
- 主技能的隊伍互動是平均值近似，不是逐次事件模擬。補能量平均攤到白天 → **對補師略微樂觀**（不會撞 150 上限浪費）。
- 絕對數值會有幾個百分點誤差。**隊伍之間的排序才是這個工具的產品承諾**，不是絕對數字。

## 上游落後遊戲

Neroli's Lab（commit 7814857, 2026-08-30）本身落後：缺脂紅火山、活力回復提升 S／M、超夢、3 道食譜。詳見 `TODO.md` 的「補上上游落後的資料」。

頁面「收錄範圍」那行刻意把實際數字印出來，不藏。
