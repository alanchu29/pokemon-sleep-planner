# 決策記錄

這個專案在一次 Claude Cowork 對話裡從零做出來（2026-09-07）。工具本身留下了，那段對話不會。這裡記的是**為什麼做成這樣**，以及**哪些數字是驗證過的、哪些不是**。

## 需求來源

使用者玩 Pokémon Sleep，每週要根據「本週島嶼加成樹果 + 本週料理主題 + 手上有的寶可夢」決定先發 5 隻。他要的不是查表，而是**窮舉最佳化**。

明確表達過的偏好：

- **中文介面** —— 他玩的是繁中版，英文對照很痛苦
- **不依賴 Claude 儲存資料** —— 要自架、要資料在自己手上（→ GitHub Pages + Google Sheet）
- **傾向按鈕／隨時要求，而非頻繁自動排程**
- 打字比下拉選單慢（中文輸入法要選字），所以 UI 以**下拉選單 + 打字跳選**為主，沒做文字批次匯入

## 關鍵設計決策

### 為什麼是窮舉，不是啟發式

C(30,5) = 142,506、C(40,5) = 658,008。把每位成員的模擬結果**記憶化**（key 為 `member × teamContext`）之後，每組隊伍的評分只是幾十次加法。26 隻實測 569 ms。啟發式沒有必要，而且會讓人不信任結果。

超過約 120 萬組合（≈43 隻）才會預篩到前 42 隻。

### 為什麼隊伍和食譜要一起最佳化

第一版是「先選食譜 → 照它挑隊伍」。這是錯的 —— 選錯食譜就得到為錯目標最佳化的隊伍。

改成：**對每組隊伍試遍所有候選食譜**，取最佳搭配再排名。實測（8 隻假資料、同一組隊伍）放開料理類型限制後，料理能量從 133k → 277k，**週總能量多 23%**。這種事看清單看不出來。

### 為什麼有兩階段（proxy → 決賽）

完整的 21 餐排程對每組隊伍跑太慢。所以：

- **搜尋階段**用 `proxyDish` —— 便宜的樂觀上界（走訪食譜填滿餐次，不追蹤食材共用）
- **決賽 50 組**跑 `bestPlan` —— 真實的貪婪排程，多重起點

`FINALISTS = 50` 是踩過坑之後定的：原本 8 組，導致真正最佳的隊伍被 proxy 擠出名單，**料理能量低估 27%**。

### 為什麼一份程式碼兩種後端

使用者要自架，但 claude.ai 上的版本也還在用（那邊有跨裝置同步、我每週一自動更新資料）。分岔兩份會立刻走鐘。所以 `!window.claude` 執行時判斷。

## 修過的 bug（都不是靠讀程式碼找到的）

| Bug | 影響 | 怎麼發現的 |
|---|---|---|
| `$` 的 TDZ | **整頁死掉** | Sheet 同步的往返測試 |
| 貪婪排程不單調 | 調高食譜等級，總分反而變低 | 對照實驗 |
| 搜尋／評分目標脫鉤 | 料理能量低估 27% | 對照實驗 |
| 幫手加速沒算樹果縮放 | 雷公／炎帝／水君被低估 **49%** | 逐項稽核程式碼 |
| 揮指／技能複製／十項全能算 0 | 這些寶可夢的主技能產出當成零 | 逐項稽核程式碼 |
| 負電無條件給能量 | 沒有正電夥伴時高估 | 逐項稽核程式碼 |
| 緞帶沒有輸入欄位 | 引擎有算但填不進去 | 逐項稽核程式碼 |
| 自架版本的文案說謊 | 按鈕聲稱 Claude 會處理，實際什麼都不會發生 | 看使用者的螢幕截圖 |

**教訓**：對照實驗（同一組輸入改一個變數）比讀程式碼有效。`tests/smoke.mjs` 就是把這些檢查固化下來。

## 驗證過的數字

以下經過交叉比對（RaenonX 資料表 + Neroli's Lab 原始碼 + namu wiki 三方一致）：

- **活力檔位**：`≥80 → ×0.45`，而且 **80 到 150 是同一格**，超過 80 不會更快。上次改動是 2024-07 的 v1.9.0，只動了低活力檔位。使用者原本以為「超過 100 會更快」—— 機制上是錯的，但**追高活力仍然正確**，因為那延長待在最快檔位的時間（起床 100 撐 3.3 小時，起床 150 撐 11.7 小時）。
- **每日活力赤字**：衰減 −144／日，睡飽只回 +100（有活力回復獎勵 105）→ **淨 −44。光靠睡覺數學上不可能維持 80 以上**，這就是補師的存在意義。
- **v3.6.0（2026-06）後的上限**：等級 70、鍋子 81、地區加成 85%、副技能欄位 Lv10/25/50/70/80。**任何寫 75/100 解鎖、鍋子 57、加成 60% 的攻略站都是舊的。**

## 兩個搜尋捷徑都驗證過了（2026-09-07）

`FINALISTS = 50` 和「>120 萬組合就預篩到前 42 隻」這兩個捷徑，訂出來之後**從來沒被驗證**。`FINALISTS` 還有前科：原本是 8，導致料理能量低估 27%。所以做了對照實驗（`searchTeams` 的 `finalists` / `prescanLimit` 參數就是為此而留）：

| 實驗 | 設定 | 結果 |
|---|---|---|
| FINALISTS 夠不夠 | 40 隻箱子（658,008 組），比 50 / 200 / 1000 | **前 8 名完全相同**，第 1 名 total 差 0.000% |
| 預篩會不會丟解 | 50 隻箱子，預篩（850,668 組）vs 全搜（2,118,760 組） | **前 8 名完全相同** |
| 預篩對抗性測試 | 60 隻箱子，幫手加速持有者與同樹果夥伴**刻意排在最後段**（讓「池子前 4 隻」都不同樹果），預篩 6.4x 砍量 | **前 8 名完全相同**，最佳隊伍照樣含幫手加速 |

第三個實驗是針對預篩的具體弱點設計的：那個「solo score」實際上是 `scoreTeam([i] + 池子前 4 隻)`，companions 是按 roster 順序取的**任意 4 隻**。幫手加速要同樹果夥伴才強，所以理論上會被低估而砍掉。實測沒有發生。

**但這不等於預篩永遠安全。** 60 隻是砍 6.4x；箱子到 100 隻是砍 100→42。要真正消除這個風險，做法是多 worker 平行化之後**直接拿掉預篩**（見 `TODO.md`）。

## 沒有驗證過的

- **從來沒跑過真實資料**。所有測試都用捏造的寶可夢。使用者的箱子在交接時還是空的。→ `TODO.md` 的第 0 項就是要補這個，**它決定其他每一項該不該做**。
- 主技能的隊伍互動是平均值近似，不是逐次事件模擬。補能量平均攤到白天 → **對補師略微樂觀**（不會撞 150 上限浪費）。
- 絕對數值會有幾個百分點誤差。**隊伍之間的排序才是這個工具的產品承諾**，不是絕對數字。

## 上游落後遊戲

Neroli's Lab（commit 7814857, 2026-08-30）本身落後：缺脂紅火山、活力回復提升 S／M、超夢、3 道食譜。詳見 `TODO.md` 的「補上上游落後的資料」。

頁面「收錄範圍」那行刻意把實際數字印出來，不藏。
