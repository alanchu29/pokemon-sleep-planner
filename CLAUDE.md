# CLAUDE.md — 專案脈絡

Pokémon Sleep 每週最佳隊伍推演工具。零依賴、無 build step、純靜態。

## 這份檔案的用途

給在這個 repo 裡工作的 Claude Code 用。上游沒有文件、原始設計討論不在 repo 裡，所以架構決策和已知陷阱都記在這裡。**動手前先讀完。**

## 架構

六個檔案，都是**真實來源**，不是產出物 —— 直接編輯：

| 檔案 | 內容 |
|---|---|
| `index.html` | 骨架：`<head>`（字型、`app.css`）＋ markup（四個 `.view`：`view-plan` / `view-team` / `view-box` / `view-recipes`，加「資料版本」footer 與 `.wrap` 之外的選擇器浮層 `#tmPicker`）＋ 尾端的載入器 |
| `src/app.css` | CSS 變數（三種主題狀態）與版面 |
| `src/engine.js` | **純引擎**，約 500 行。window 與 Worker 兩邊都載入同一份 |
| `src/engine.worker.js` | 薄薄一層 Worker 外殼，約 50 行 |
| `src/import.js` | **截圖匯入的反解層**，約 210 行。純函式，只在主執行緒載入（Worker 不需要） |
| `src/app.js` | UI 與持久層，約 950 行 |
| `data/game.json` | 遊戲資料快照。246 隻寶可夢、78 道食譜、性格／副技能／主技能數值、`zh` 繁中對照表、`meta` 版本戳。**indent-2 pretty-print，一個欄位一行** |

### 載入順序（重要）

`index.html` 尾端的 `<script type="module">`：

1. `await fetch('./data/game.json', {cache:'no-cache'})`，檢查必要的頂層鍵都在 → 放到 `window.GAMEDATA`
2. 依序動態插入 `./src/engine.js`、`./src/import.js`、`./src/app.js`（**classic script，不是 `import()`**）

`engine.js` 宣告 `D` 和所有引擎函式；`import.js` 用 `engine.js` 的 `baseStats` / `helpInterval` 做截圖反解；`app.js` 直接用前兩者的全域名字，**自己不要再宣告 `D`**（同名 `const` 會撞成 SyntaxError）。

**為什麼是動態插入 classic script 而不是 `import()`**：頂層宣告必須留在全域。`tests/smoke.mjs` 靠 `page.evaluate` 直接驅動內部狀態（`roster = [...]`、`run()`、`scoreTeam()`、`buildPool(wk)`），改成 module 會把這些關進模組作用域，整套測試會全滅。**不要「順手」改成 module。**

因為用了 `fetch`，**`file://` 直接開會失效**（CORS）。本機要跑 `npm run serve`。載入器有 catch，會顯示提示而不是白畫面 —— 改動載入器時要保留這個 fallback。

### Worker 池

推演分片跑在 `MAX_WORKERS`（預設 6）個 `src/engine.worker.js` 上。主執行緒和每個 worker **載入同一份 `engine.js`**，所以不會有多份引擎走鐘的問題。

```
app.js  run()   ──{init, data:D}──▶  engine.worker.js × N
                                        └─ importScripts('./engine.js')
        ──{shard:{index,total}, roster, wk}──▶  searchShard(...)  ← 只列舉與評分
        ◀──{progress, done, total}──────────    （每 80ms 節流，主執行緒加總）
        ◀──{shard, cands:[{idxs,score}], ms}──  精簡候選

        合併 → 取全域前 FINALISTS → rehydrate() → finalizeTeams()   ← 都在主執行緒
```

**分片的正確性**：全域前 N 名必然也在各自分片的前 N 名內，所以合併集合一定包含它們 —— 分片結果與單執行緒窮舉**完全相同，不是近似**。`tests/smoke.mjs` 第 10 節斷言兩條路徑逐欄位相同。

五條規則：

1. **`engine.js` 絕對不能碰 DOM，也不能讀 `app.js` 的狀態**（`roster` / `wk` / `lastResults`）。需要什麼就當參數收 —— worker 裡沒有那些全域。`rlvl(r, wk)`、`buildPool(wk)`、`rankRecipesForTeam(r, wk)` 的 `wk` 參數就是為此而加的，**不要改回讀全域**。
2. **決賽（`bestPlan`）只能在合併之後跑一次。** 每個 worker 各跑一遍是白費工，而且各自只看到自己的候選集合。
3. **worker 回傳精簡候選（`{idxs, score}`），主執行緒用 `rehydrate()` 還原。** 完整的 `scoreTeam` 結果一個分片 241 KB，8 個分片 1.9 MB，實測那就是平行化的主要瓶頸。`scoreTeam` 是決定性的，所以還原出來的與 worker 端算的相同。
4. **取消是靠主執行緒 `terminate()` 砍掉整池，不是傳訊息。** 搜尋是同步迴圈，跑的時候 worker 不會處理訊息佇列，送 `cancel` 進去要等搜尋結束才被讀到。`SharedArrayBuffer` + `Atomics` 可以真正中斷，但需要 COOP/COEP 標頭，GitHub Pages 給不了。`killPool()` 也會主動 reject 還在等的 promise，否則每次取消都留下永不 settle 的 async 呼叫。
5. **主執行緒也需要 `POOL` 和 `_bs`**。`renderResults` 會呼叫 `rankRecipesForTeam`（吃 `POOL`）、`memberCard` 會讀 `m._bs`，`rehydrate` 兩者都要。worker 算的那份在它自己的記憶體裡，所以 `run()` 在送出之前會自己再算一遍（很便宜）。

`Worker` 不可用或載入失敗時會**退回主執行緒**跑 `searchTeams()`（UI 會凍住，但至少有答案），`comboCount` 會標上「· 主執行緒」。

**加速只有約 2x，而且瓶頸不在程式碼**：分片是平衡的（各分片耗時差 1.06~1.11x），編排開銷 27~46ms，飽和點在 6 個 worker。量測與「為什麼不做動態工作竊取」見 `DECISIONS.md`。

**⚠ dedicated worker 和主執行緒共用同一個 renderer 行程，所以記憶體是加總的。**
引擎裡任何「每組合都會長一點」的東西（最典型的是 `memberOutput` 的 memo）都要**乘以
六**才是真正的用量，而撞到上限的症狀是分頁直接 `Out of Memory` 崩掉、不是變慢。
實際踩過（2026-09-10，89 隻的箱子跑到 25%）——原因與判準見陷阱 6。
箱子大小是**組合數**：60 隻 5.5M、80 隻 24M、**89 隻 41.5M**、100 隻 75M。

### 截圖匯入（`src/import.js` ＋ 寶可夢箱的「從截圖建立」）

遊戲的寶可夢詳細頁上**沒有物種名**（只有使用者取的暱稱和糖果名），也**沒有睡眠緞帶**。但畫面上有兩個衍生數字：

```
幫忙間隔 每31分51秒        持有上限 35個
```

兩個都是 `(物種, 等級, 性格, 副技能, 緞帶, 露營券)` 的封閉式函數，而引擎已經有那兩條公式。所以不辨識物種名，反過來**掃 246 隻 × 5 種緞帶求解**：哪一組能同時算出畫面上那兩個數字。

實測**七隻**真實寶可夢（清單見下方「驗收方式」）全部都是**唯一解**，而且任何單一欄位讀錯（等級 ±1、性格、漏看一個幫忙速度副技能、持有上限 ±1、幫忙間隔 ±1 秒、食材數量 ±1）都會變成**無解**，不會變成錯的答案。

三個附帶好處：

1. **它驗證整筆讀取。** 校驗通過幾乎等於整筆正確。
2. **它反解出畫面上看不到的緞帶。** 只靠 `RIBBON_CARRY = [0,1,3,6,8]` 的差異。
3. **它自動決定進化階段。** 喇叭芽／口呆花／大食花的食材組合、主技能、樹果、糖果名全都相同，但基礎頻率是 5200／3800／2800 —— 只有 2800 能算出 1911 秒。

四條規則：

1. **絕對不要在 `import.js` 複製引擎的公式。** `impInterval` 呼叫 `helpInterval`、`impCarry` 呼叫 `baseStats().carry`。`helpInterval` 就是為此從 `simulate` 抽出來的具名函式 —— 兩份公式一定會走鐘，而走鐘的那份會靜靜地算錯。
2. **自動判斷只產生草稿，不寫 `roster`。** 校對表的每一欄都是可編輯的控制項（種類／等級／性格／五格副技能／三格食材／技能Lv／緞帶），改動會即時重跑 `impVerify`，按「存入箱子」才 `roster.push`。`tests/smoke.mjs` 第 11b 節會斷言「確認之前 `roster.length` 不變」。
3. **反解不出唯一值的欄位一定要標出來**（`.amb` 橘框 ＋ 備註）。靜靜地填一個猜的值就是「文案說謊」那類 bug。
4. **缺欄位要老實變成多解。** 沒填兩個校驗碼就會列出幾十組候選並警告，不准挑一個看起來確定的。

### 程式碼分區

`src/engine.js`（依出現順序）：

1. `const D`（= `self.GAMEDATA`，加完整性檢查）＋ 資料衍生常數：`ING_NAME` `ING_VAL` `NING` `BERRY_VAL` `NAT` `SS` `SS_SLOT_LV` `RIBBON_CARRY` `AVG_CRIT` `HB_TABLE` `MAGNET_POOL` `MEALS_WEEK`
2. **引擎**：`energyF` `berryPower` `baseStats` `skillPayload` `simulate` `memberOutput` `teamContext`
3. **食譜求解**：`buildPool` `rankSingle` `bestSingleRecipe` `mealPlan` `bestPlan` `scoreTeam` `rankRecipesForTeam`（曾經有個 `proxyDish` 樂觀上界，已移除，見下方陷阱 5）
4. **個體產能**（只給箱子的 UI 用，不參與推演）：`SCORE_WK` `SCORE_CTX` `monPower` `POWER_MAIN` / `powerMain` `monPowerCached` `monIdeal`
5. **搜尋**：`combinations` `searchTeams`（＋ `FINALISTS` / `SHOWN`）。一律窮舉 —— 曾經有的 `PRESCAN_LIMIT` 預篩已移除，理由見下方陷阱 4

`src/import.js`（依出現順序）：

1. `impNorm`（全形→半形正規化）＋ `impRev` 反向索引，及 `impNature` / `impSubskill` / `impMainSkill` / `impIngIndex`
2. **校驗碼**：`impInterval` `impCarry` `impSecs` —— 都是**呼叫引擎那一份公式**，不自己算
3. **主技能等級**：`impSkillBonus` `impPayloadHits` `impEffFromPayload` `impSkillLv`
4. **食材欄位**：`impIngSets`（**三格都解，包含還沒解鎖的那格**）
5. **求解**：`impSolve`（掃 246 隻 × 5 種緞帶）、`impVerify`（UI 改欄位後即時重驗）

`src/app.js`（依出現順序）：

1. `const $` — DOM 輔助（**必須在最前面**，見下方陷阱）
2. `PATHS` / `P` / `C` — 文案裡提到的檔名與指令一律從這裡取（見下方陷阱）
3. `SCHEMA` ＋ `fatal()` ＋ schema 斷言
4. 中文查表：`bz` `iz` `pz` `isl` `msz` `ssz` `sss` `natZ` `recipeZh`
5. **狀態**：`roster` `wk` `lastResults`
6. **持久層**：雙後端配接器（見下）
7. **Worker 管線**：`spawnWorker` `killWorker` `searchViaWorker` `setRunning`
8. **UI**：`buildWeekly` / `weeklyChanged`、`ingPick` / `monCard` / `setMonValues`（**寶可夢箱與截圖校對區共用同一個卡片渲染器**）、`boxFlt` / `monMatch` / `monHaystack` / `applyBoxFilter` / `renderBox`、`buildImport` 那一組、`run`、`ingUtilNotice` / `teamDetailHTML` / `bindTeamDetail`（**推演與自組隊伍共用的結果渲染器**）、`renderResults`、自組隊伍那一組（`computeTeam` / `renderTeamsView` / `openPicker`…）、`renderRecipeLevels` `showView` `renderVersion`

### 自組隊伍（`view-team`）

手動指定 5 隻看數字，而不是讓推演去找。和推演是「同一件事的兩種模式」，所以分頁排在它旁邊。

**引擎層本來就支援** —— 📌 固定 5 隻時 `combinations` 的 `need === 0` 只列舉一組。這個分頁做的是 UX：不用去箱子裡一隻一隻找、按 pin、算完再清掉。

六條規則：

1. **計算基礎百分之百比照推演。** `computeTeam` 就是 `scoreTeam` ＋ `finalizeTeams([r], roster, wk, 1)[0]` —— **不自己重寫決賽排程**。走鐘的後果是兩個分頁對同一支隊伍給出不同的總能量，那比沒有這個功能更糟。第 11j 節斷言「自組 = 推演，逐欄位」以及「= 把那 5 隻 pin 起來跑推演」。
2. **結果渲染器只有一份**（`teamDetailHTML`）。和 `monCard` 同時給箱子與截圖校對區用同一個理由。`opts.rosterLabel` 只換那行小標題 —— 推演是「建議先發 5 隻」，自組隊伍講「建議」就是文案說謊。
3. **湊滿 5 隻才算。** `teamContext` 的 `energyTeam*5` 與 `qE(energy/5)` 兩邊都寫死 5 人，不足 5 隻算出來的數字沒有意義。**不要為了試算中間態去把那個 5 改成 `idxs.length`** —— 那會改動所有既有推演結果的數值，而遊戲裡隊伍本來就是 5 隻。
4. **`members` 存 roster 索引**，所以和 `monOpen` 是完全一樣的陷阱：箱子刪除要 `teamsAfterDelete(i)`（被刪的清空、後面前移），整批取代要 `teamsReset()`。漏掉不會有任何錯誤訊息，只是靜靜地指到別隻。`sanitizeTeams()` 是最後一道防線。
5. **不進 `serialize()`。** 純檢視狀態，和 `boxFlt` / `monOpen` 同待遇 —— 切分頁保留、重新整理清空，所以 `SCHEMA` 不用動。**這件事要寫在分頁標題上**，否則使用者會以為隊伍存起來了。
6. **`strictBerry` 不擋手動選，但要講出來。** 它是候選過濾規則，手動隊已經指定了 5 隻所以自然不生效（使用者的立場：手動權力最大）。但那會造成「這裡算得好好的、推演卻永遠不推薦」的矛盾，所以隊中有不符的樹果型時要顯示 `.notice` 並寫出是哪一隻 —— 「靜靜地少算候選」的鏡像。

**選擇器是浮層（`#tmPicker`），不是內嵌展開。** 這個分頁的核心動作是「換掉一隻馬上看數字怎麼變」，內嵌展開每開一次就把正在看的結果推走，每次換人都要重新找回視線位置。它放在 `.wrap` **之外** —— `position:fixed` 的祖先只要有 `transform`／`filter` 就會變成定位容器。`showView` 切走時要 `closePicker()`。

搜尋走 `monHaystack`（**和箱子的篩選列共用**，暱稱＋學名都吃），排序沿用 `boxOrder()`，但**篩選條件是選擇器自己的**（`pickerQ` / `pickerSpec`）—— 吃 `boxFlt` 的話會出現「箱子篩了食材 → 這裡莫名少了一半」。

**同隊不可重複、跨隊可以。** 同一隻放進同一隊兩次會讓 Helper Boost 的物種計數、流星群的龍屬性種類數全部算錯；跨隊重複則是必要的（比較兩隊通常只換 1~2 隻），已在別隊的要標出來。

### 鍋子的剩餘空位會被別的食材填滿（`mealPlan` 的填充段）

**遊戲機制（使用者 2026-09-09 確認）：湊齊食譜需要的食材之後，還可以繼續把別的食材塞進鍋子，直到鍋子容量上限，那些食材的能量會直接加進這道料理。**

以前完全沒有這一段 —— 一道只要 23 個食材的食譜，在容量 81 的鍋子裡就只用 23 個，剩下 58 格空著。實測影響：使用者那一週的料理分數 **187k → 383k（+104%）**，而且推薦陣容從「2樹果1食材2技能」變回「3樹果1食材1技能」。

四條規則：

1. **填充放在所有食譜排完之後。** 那樣填充不會搶走食譜需要的食材 —— 排程仍然由「單道能量最高」決定（食譜的 `rlb × bonus` 倍率約 2.4 倍，遠高於食材的原始能量），填充只是把剩下的變現。結果是可達的，所以 `mealPlan` 仍然是下界（陷阱 5 靠這件事）。
2. **能量高的食材先塞。** 空位有限，同樣一格當然放值錢的。
3. **兩種食材的計分方式不同**（遊戲機制，使用者確認）：

   | | 食譜等級 `rlb` | 食譜加成 `bonus` | 大成功 | 島嶼加成 |
   |---|---|---|---|---|
   | 食譜規定的基礎食材 | ✓ | ✓ | ✓ | ✓ |
   | 額外塞進去填鍋的 | ✗ | ✗ | ✓ | ✓ |

   額外食材**只算原始基礎單價**（130 分的火辣香草丟進 Lv1 的蘋果汁或 Lv60 的馬卡龍都是 130），但大成功與島嶼加成作用在**整鍋總和**上 —— 程式上就是 `fillE * mul`（`mul = critMul × areaMul`），不乘 `rlb` 也不乘 `bonus`。**曾經一度改成吃食譜倍率，那是錯的，已還原。**
4. **`mp.fillE` 回傳的是已經乘過 `mul` 的分數**（和 `total` 同單位），`fillN` / `room` 給 UI 顯示。**21 餐排程表一定要有「鍋子空位填入其他食材」那一列** —— 否則表格逐列加起來會少一截而看不出少在哪。第 2b 節斷言 `Σ食譜 + fillE === mp.total`。

**空餐（`idleMeals`）仍然不計分。** 那是拌拌料理，遊戲裡有能量但快照沒有係數 —— UI 寫明「這個工具不計分」。

### 食材利用率（`ingUtilNotice`）：診斷歸錯原因比不提示更糟

以前 UI 只在食材**不足**（`mp.idleMeals > 0`）時警告，但實際更常見的是相反：食材大量過剩而畫面一片安靜 —— 使用者只會覺得「推演怎麼都不選食材型」。低於 `ING_UTIL_WARN`（75%）就出聲，推演與自組隊伍共用同一份。

**這一節的重點是：這段文案在一天之內錯了兩次，兩次都是把原因歸錯。**

| 版本 | 文案說的原因 | 為什麼錯 |
|---|---|---|
| 第一版 | 「21 餐 × 鍋容量吃不下，要提高鍋子容量」 | 當時沒有填充機制，鍋容量根本沒用滿（21×81 = 1701，只煮掉 1373）。使用者當場算出來 |
| 第二版 | 「不是鍋子太小，是食材種類湊不齊（木桶效應）」 | 正確描述了當時的模型，但**模型本身漏了填充機制** |
| 現在 | 「空位也全部塞滿了 → 加大鍋子容量，或改煮食材數較少的食譜」 | 填充補上之後，鍋容量**真的**是瓶頸 |

**用量一定要拆成兩段**：`食譜指定 N ＋ 填進鍋子空位 M`。使用者原本的困惑（「一週產 1393、一餐可以用 81，為什麼只煮掉 519」）就是因為填充那一段根本沒算。

診斷的判斷方式（**不能從利用率反推**）：

1. **`roomLeft = room − fillN <= 0`** → 每一鍋的空位塞滿了 → 加鍋容量或改煮食材數較少的食譜。這是填充機制加上之後的主要瓶頸。
2. **`POOL` 前段有 `cnt > potEff`** → 還有更貴的食譜連進鍋的機會都沒有。這是**另一件事**，早一版把兩者混在一起會同時說出矛盾的兩句話。
3. **`idleMeals > 0`** → 連便宜的食譜都湊不齊食材（木桶效應：`cooks = min(floor(pool[i]/a))`）。

**「這隊最能煮的食譜」表有一欄「卡在」**（`rankRecipesForTeam` 的 `bn`，本來就算了只是沒顯示）—— 直接回答「為什麼這道煮不了」。

### 食譜要「解鎖」才算數（`recipeOn`）

**有填等級 ＝ 已解鎖；沒填 ＝ 還沒解鎖，整個排除。**（使用者 2026-09-10 指定）

以前沒填的會退回 `wk.recipeLv` 那個預設等級 —— 等於假設「你 78 道全都會煮」，於是推演會圍著使用者**根本煮不出來**的食譜配隊，而畫面上完全看不出來。遊戲裡食譜要煮過才解鎖，沒解鎖的等級欄本來就是空的。

**單一真實來源：`wk.recipeLevels[r.n]` 的有無。** 不另外開一份「停用清單」—— 同一件事有兩個來源就一定會有一個在說謊（和「文案不能寫死檔名」同一類）。所以「鎖上」＝ 刪掉那個 key，「解鎖」＝ 填入 `wk.recipeLv` 當起點。

這是**產品規則，不是效能手段**（陷阱 4），所以那三條配套一樣要守：

| 配套 | 在哪裡 |
|---|---|
| 排除的道數要看得見 | 結果頁 `comboCount` 的「食譜 N/78 道已解鎖」＋ 食譜頁的 `rlvCount` |
| 一道都沒解鎖要出聲 | `#rlvNone` 警告條 —— **不可以靜靜地把料理算成 0**，那是個懸崖 |
| 選單不能列出煮不出來的 | `fillRecipes()` 只列已解鎖的；一道都沒有時 option 自己講出來 |
| **撞牆時的訊息要指名真正的原因** | `RUN_ERR.nopool` ＋ 自組隊伍的 `t.blocked`（見下） |

#### ⚠ 「一道都沒解鎖」**就是全新使用者的預設狀態**（2026-09-10 補）

`wk.recipeLevels` 的預設值是 `{}`，所以第一次打開這個工具的人 **一定**會撞到這面牆
（不像 `wk.fav` 有 `if (!wk.fav.size) wk.fav = new Set([...])` 那種首次預設）。
三處文案以前都在講別的事：

| 在哪 | 以前寫的 | 為什麼是謊 |
|---|---|---|
| 推演分頁 | 「目前的料理類型／範圍下沒有任何食譜可比較」 | 把人推去改「料理類型」和「考慮範圍」兩個下拉，怎麼改都一樣 |
| `#rlvNone` | 「推演的**料理分數會是 0**、21 餐全部空著」 | `run()` 在 `buildPool` 之後就直接拒跑，根本不會產出任何結果 |
| 自組隊伍 | 卡片「**還差 0 隻**」＋ 詳情「湊滿 5 隻才會算出結果」 | 五格明明都填滿了 |

那個「還差 0 隻」的來源是 `computeTeam(t, ready)` 把兩種 null 混在一起。現在多一個
`t.blocked`（＝人湊滿了但 `prepTeamCalc()` 失敗），卡片與詳情各自分岔。
第 2e 節斷言這四句話。**再加任何一種「算不出來」的原因時，這兩處都要跟著分岔** ——
把不同原因收斂成同一句「還差 N 隻」就是這一類 bug 的溫床。

四件事：

1. **沒解鎖的那幾列照樣顯示，只是變淡**（`.rlv-off`），倍率與單道能量換成「—」。和箱子裡未解鎖的副技能／食材格同一個處理方式：藏起來就看不出「為什麼它沒被算」，顯示成正常數字又是說謊。
2. **`wk.recipeLv` 的語意變了。** 它不再是「沒填的用這個」，而是**「按解鎖時先填這個」**，所以標籤改成「新解鎖時的等級」。舊標籤留著就是文案說謊。
3. **`deserialize` 一定要正規化（`reviveRecipeLevels`）。** 改動之前，一個 `"20"` 字串頂多讓等級不準；改動之後，同一個髒值會讓**那道菜整個從推演裡消失**。**改動讓既有失效模式變嚴重時，就要在入口補一道正規化。**
4. **`rlvl` 不管解鎖與否**，它只回答「等級是多少」；要不要納入由 `recipeOn` 決定。食譜頁會拿它顯示「解鎖之後會是幾級」，所以那個 `wk.recipeLv` 的退路要留著。

批次的「全部解鎖／全部鎖上」**只作用在目前篩選看得到的那幾道**（和箱子的「展開／收起全部」同一條規則）。「全部解鎖」不會蓋掉已經有等級的；「全部鎖上」會清等級，所以要 `confirm` 並寫出是幾道。第 11p 節有斷言。

### 「多久該上去收一次」（`collectRow` ＋ 建議收取間隔 pill）

推演結果與自組隊伍的成員卡上有一行「背包裝滿 N」，整隊那一排 pill 上有一顆
「建議收取間隔 N」。兩個都由 `simulate` 回傳的 `fillH` / `skillH` 算出來。

**兩個天花板都要講，只講背包會誤導技能型。**

| | 到頂之後 | 時間 |
|---|---|---|
| `fillH` 背包裝滿 | 只剩樹果，**食材歸零** | `helpsTillFull ÷ 白天每小時幫忙次數` |
| `skillH` 主技能存滿 | 再發動也拿不到（技能專長 2 次、其他 1 次） | `(bankedProcs / effSkill) ÷ 同上` |

哥達鴨（技能觸發率 20.4%）**先到的是後者**：技能 4.6h 就存滿，背包還要 8.2h。只顯示
背包時間的話，中間那 3.6 小時看起來像什麼都沒漏 —— 那正是陷阱 6e 那個偏差的來源。
低頻的達克萊伊剛好相反（背包 7.3h、技能 9.8h）。所以成員卡上 `skillH < fillH` 時
會多接一段「技能存滿 N」。

三件事：

1. **換算用白天的平均速度**（`helpsDay / wakeH`），夜間不算 —— 那一段沒辦法中途收，
   它的損失本來就在 `nightSnack` 裡（也就是旁邊那個「背包滿 N」）。起床時活力最高、
   實際比平均快一點，所以這是**偏保守**的估計，tooltip 要寫出來。
2. **`fillH` / `skillH` 不可以跟著 `wk.collectH` 跑。** 它們是「這一隻能撐多久」，
   不是「你設定收多久」的函數 —— 跟著跑的話使用者一調設定建議就跟著動，那個建議
   就沒有意義了。第 11q 節直接斷言 `collectH` 4／6／0 三種設定下數值完全相同。
3. **pill 由最先到頂的那一隻決定**（`min` over 成員），因為一次上線是全隊一起收。
   只給每隻的數字而不給最小值，等於要使用者自己算。設定的間隔比建議長時，pill 與
   那幾隻的成員卡都要標紅加 ⚠，而且說明要寫出**推演已經把溢出扣掉了**（不是預估）——
   否則使用者看到分數低於預期卻不知道為什麼。


**⚠ 跑單調性測試時要注意 `collectH` 會改變靈敏度。** 同一個有問題的改動（`bestPlan` 全起點窮舉，已退回），在不同 `collectH` 下測出來的結果完全不同：

| `collectH` | 單調性 |
|---|---|
| 沒設（＝隨時收）／ 0.5 | **下降 5/40，最多 −1.405%** |
| 3 / 4 / 6 / 15.5 | 0/40 ✓ |

`collectH` 大的時候主技能被「每段最多 2 次」cap，分數差距拉開，臨界情況剛好被蓋住。**所以「在預設設定下測過了」不構成證據** —— 動 `mealPlan` / `bestPlan` 時，單調性測試至少要涵蓋一個很小的 `collectH`（或不設，＝隨時收）。`tests/smoke.mjs` 第 3 節用的是 app 的預設值，那一格是測不出來的。

### 結果卡的「為什麼選這一隻」（`pickReason`）

**每一句都從實際算出來的貢獻回推**，對應得到卡片上看得到的數字 —— 否則就是無法查證的形容詞，那比不解釋更糟。主力那一項用「佔隊伍該分項的比例」而不是絕對值：三種產出的單位不同（樹果／主技能是卡比獸能量，食材是還沒經過料理放大的原能量），絕對值不可比。

**代價也要寫**（夢魘的扣活力）—— 只講好處就是選擇性呈現。**而且它不佔那三格**：`cost` 另外接在後面，前面只留兩句。以前它排在最後、被 `slice(0, 3)` 砍掉過。

**只放得下三句，所以「挑哪三句」本身就是一個會說謊的地方。** 排序的準則不是「哪一句好聽」，而是**「這一句在卡片上還有沒有別的地方看得到」**：

| 組 | 內容 | 別處看得到嗎 |
|---|---|---|
| `head` | 主要產出佔隊上幾成 | 卡片右邊就是那些數字 |
| `team` | 牠**對隊友**做了什麼，而且有數字（補活力／多幫忙） | **沒有** —— 底下那排 pill 只給整隊合計，看不出是誰供的 |
| `own` | 牠自己的其他量化貢獻（瓶頸食材／自回活力） | 部分 |
| `flag` | 靜態標籤（加成樹果／幫忙加成／活力回復提升／幫手加速） | pill 或名字旁的 tag 也看得到 |

**踩過（2026-09-10，使用者反映「這個推演結果有點詭異」）：** 以前是一條固定順序推進同一個陣列再 `slice(0, 3)`。純補師（胖可丁，活力全體療癒S）的卡片上因此留下「食材 17k／週　佔這隊食材的 9%」，而「每日補活力 每隻 90」排在第 4 被砍掉 —— **那是牠入選的唯一理由**：實測同一個位置換成非補師，隊伍週能量 1,097,922 → 923,140（**−18.9%**），另外四隻的活力從 100% 全部掉到 0%。畫面於是在解釋一個無關緊要的數字，把真正的理由藏起來。第 11n 節有斷言。

**畫面上不准出現原始碼的變數名，標籤本身要說得出它在講什麼。** 實際被問過的四個：

| 原本 | 問題 | 現在 |
|---|---|---|
| `HB ×3` / `ERB ×0` | `nHB` / `nERB` 的縮寫，那是變數名 | 幫忙加成 ×3 / 活力回復提升 ×0 |
| 幫手 88.0／日 | 看不出是什麼的次數 | 幫忙 88.0 次／日 |
| 偷吃 6.0 | 自創詞，遊戲裡沒有 | 背包滿 6.0 |
| **最快檔位 17.2h／日** | **字面上完全看不出在講活力** —— 加了 tooltip 使用者仍然看不懂 | **活力80以上 17.2h／日** |

最後那個是重點：**tooltip 補不回一個誤導的標籤**。「最快檔位」講的是 `energyF` 的五個活力檔位（80+ ×0.45 最快 → 0 ×1.00 最慢），標籤裡沒有「活力」兩個字，讀者根本不知道要往哪裡想。tooltip 現在除了檔位表還寫「比例低就是活力不夠，考慮帶補師或睡久一點」—— **說出該怎麼辦，不只是定義**。

### 寶可夢箱的卡片版面：摺疊列 ＋ 展開編輯

**預設摺疊成一行唯讀摘要，點一下才展開編輯區。** 這是兩次修正之後的結論：

1. 原本是一列八欄的 grid，實際寬度下每個選單都被裁掉（`大食花　#71 Vi…`、`技能…`、`頑皮 +速度` 都看不完）。而這個工具的可信度就建立在「畫面上寫的就是實際採用的值」—— 看不完等於沒有。
2. 改成三列全展開之後可以看完了，但**一隻就吃掉半個螢幕** —— 60 隻的箱子根本沒辦法掃。

所以摺疊列是 **2 欄 × 2 列的 grid**：

| | 欄 1 `.mon-idy`（**固定 206px**） | 欄 2 |
|---|---|---|
| 列 1 | `▶ #圖鑑號 暱稱 Lv` | `.mon-rest`：`[專長] [主技能] 性格 副技能… 技Lv 產能 名次 練滿 技能+N [＋隊 📍 ✕ 🗑]` |
| 列 2 | `.mon-idl`：**資質 N%**（見下） | `.mon-ings`：**只有食材** |

全部唯讀、有顏色；展開後才有選單。

**欄 1 為什麼是固定寬而不是 `max-content`：** 要的是**所有卡片的欄 2 都從同一個 x 開始**，食材那一列才會對齊上一列的專長標籤。`max-content` 會讓每張卡的起點隨名字長度浮動，掃 60 隻時反而更亂。名字太長就在欄 1 裡自己折，**不裁切**。窄螢幕（≤700px）退回單欄堆疊 —— 那個寬度下對齊沒有意義。

**`.mon-ings` 一定要寫 `grid-column:2`**，不然 grid 的自動排版會把它丟到欄 1。

**食材為什麼要獨立一列：** 原本副技能和食材同在一個 `.mon-sum` 裡靠 flex-wrap 自然換行，結果兩種標籤混在同一列、而斷行位置隨視窗寬度浮動 —— 掃 60 隻時分不出哪個是哪個。

### 自訂暱稱（`m.nick`）

遊戲的寶可夢詳細頁**沒有物種名**，所以使用者認得的是自己取的名字（「樹果萌萌」而不是「嘎啦嘎啦」）。`nick` 是純標籤，**引擎完全不看它**。

- 摺疊列顯示暱稱（帶虛下框 `.is-nick` 標示「這不是學名」），空的就退回 `pz(p)`。物種身分不會消失：`#圖鑑號` 在同一列、`title` 寫著「暱稱「…」 · 學名 …（…）」、展開後「種類」選單就是學名。
- **推演結果卡並列暱稱與學名**（`.nm-sci`）—— 箱子裡有兩隻妙蛙花時，選中的是哪一隻只有暱稱分得出來，但學名也一定要在，不然不知道要看哪一隻的數值。
- 搜尋列要**同時**吃暱稱和學名。只吃暱稱等於這個功能只做一半（打「嘎啦嘎啦」找不到取了名的那隻）。
- `dupKey` **包含暱稱**：數值一模一樣但取了不同名字，那是兩隻不同的個體，不該標成重複；同一隻的截圖看了兩遍，暱稱也一樣，照樣抓得到。
- `rosterTable()`（Sheet 的可讀鏡像）暱稱放**第一欄**。

### 個體產能（`monPower` / `monIdeal`）—— 只是顯示

**推演一行都沒改。** 產能只出現在寶可夢箱，`searchTeams` / `scoreTeam` 完全沒動。

**核心規則：三種專長的分數不可互相比較。** 職責不同、單位就不同：

| 專長 | 主指標 | 為什麼是這個 |
|---|---|---|
| 樹果 | 總產能能量／日（樹果＋食材＋技能） | 樹果型負責的就是穩定產能 |
| 食材 | 食材原始能量／日（未經料理加成） | **刻意不看「配不配本週食譜」** —— 那正是每週推演在做的事，而且會週週跳動 |
| 技能 | 主技能發動次數／日 | 不同技能給能量／食材／幫忙／夢之碎片，換算率是憑空的判斷；發動頻率是共同的軸 |
| 全能 | 總產能能量／日 | 同樹果 |

所以 UI 上**每個數字旁邊都必須有專長標籤與單位**，而 `BOX_SORTS.power` 是**先分專長、再組內比** —— 把三種混在一起排，那個排名本身就是謊話。同專長內的名次（`食材 3/12`）反而是可以跨專長讀的。

基準：`SCORE_CTX` 是**單獨一隻、沒有任何隊友加成**（`nHB = 0`）—— 就是遊戲寶可夢詳細頁顯示幫忙間隔時的情境，所以畫面上的數字對得起來。`SCORE_WK` 固定（無露營券、睡 8.5 小時、**不含本週加成樹果**），所以跨週可比。

四條規則：

1. **量不到的東西要標出來，不要假裝算進去了。** 「幫忙加成」的價值主要在加速四個隊友，單獨一隻只看得到自己那 5% —— 所以 `monPower` 回傳 `teamOnly`，UI 標「隊伍型」徽章。推演會正確計入。
2. **`monIdeal` 的目標函式必須是那個專長的主指標**（`powerMain`）。用總產能去挑技能型的理想個體會挑出完全不同的一組副技能。
3. **`monIdeal` 的最後一步一定要把「牠自己」放進候選。** 貪婪是逐格挑的，可能漏掉有交互作用的組合；漏掉時理想值會低於實際值，百分比超過 100%，看起來像壞掉。這也是 `idealCache` 的鍵是整隻的簽章而不是「物種｜等級」的原因。
4. **基準一定要顯示**（`scoreNote()` → `#scoreNote`）。一個沒有出處的數字比沒有數字更糟，而「暗示可以互相比」比沒有數字更糟。

   但完整說明有 300 多字，攤在篩選列和箱子列表中間會把真正要看的東西推到畫面外（使用者 2026-09-09 直接反映「介紹太多了」）。所以拆成兩段：

   | | 內容 | 為什麼在這一邊 |
   |---|---|---|
   | **摘要（永遠顯示，`.sum`）** | 基準情境（單獨一隻、不含本週加成樹果）／三種專長不能互相比較／**三個投資數字各答哪個問題（練滿・資質・技能）**／不影響推演 | **漏掉任何一件都會讓人讀錯數字**，摺起來等於沒有 |
   | **摺疊（`<details class="more">`）** | 為什麼是那三個軸、為什麼基準是 Lv60、為什麼緞帶與技能等級也要規範化、`≥` 是什麼意思、隊伍型徽章 | 是「為什麼」，讀過一次就不用再讀 |

   **「三個數字各答哪個問題」是摘要裡最不能省的一句。** 使用者會拿摺疊列上能排序的
   數字去決定練誰；少了這句話，介面本身就在請人用錯的軸做投資決定（見下一節）。

   **摺疊狀態要記在 `scoreNoteOpen` 裡。** `renderStorageNote()` 會在雲端同步成功時重跑並整段 `innerHTML` 重建 —— 不記住的話會「讀到一半自己收起來」。它是純檢視狀態，和 `monOpen` 同待遇，不進 `serialize()`。

   測試看的是 `textContent`，摺疊不影響（`<details>` 收著時文字仍在 DOM 裡），所以第 11f／11i 節既有的斷言照樣有效 —— 但**要另外斷言那幾件事在摘要那一行裡**（第 11g／11L 節），否則哪天有人把它們搬進摺疊區，測試不會紅。

### 寶可夢箱的三個投資數字：練滿／資質／技能成長（`idealChip` / `investChip`）

`monIdeal` 一次算出三個數字，**各對應一種資源**，回答三個不同的問題：

| 顯示 | 算式 | 回答 | 可比範圍 | 排序 |
|---|---|---|---|---|
| **練滿 X**（`.mon-full`） | `powerMain(ideal.self)` | **等級糖果先餵誰** | 同專長內（和當前產能同單位） | `BOX_SORTS.full` |
| **資質 N%**（`.mon-idl`） | `ideal.self ÷ 理想個體` | **這一隻是不是好貨** | **同物種內** | `BOX_SORTS.ideal` |
| **技能 +N**（`.mon-room`） | `ideal.self.total − ideal.skillNow.total` | **技能糖果先給誰** | 全體（同一種資源） | `BOX_SORTS.skillRoom` |

三者的基準完全相同：**Lv60 以上（超過就用實際等級）、緞帶 4、主技能滿級**。

實測三者確實彼此獨立：電龍資質只有 64%、技能成長 +22,830；大食花資質 67%、技能成長只有 +283。混成一個數字就會同時答錯三個問題。

**為什麼「練滿」非顯示不可、而且非能排序不可（2026-09-10，使用者反映）**

以前摺疊列上唯一能排序的推算值是「潛力 %」（＝現在的資質）。使用者會很自然地按它排序，然後**不去優先練顯示低分的那幾隻** —— 但那是**只在同物種內有意義的比值**，拿來跨物種決定「先練誰」是錯的軸：地板隨物種浮動（實測 34.6%~55.2%），所以 60% 的袋獸幾乎貼著地板，60% 的耿鬼卻是舒服的中段。

「該練誰」的答案是**絕對值**，而引擎本來就算好了（`ideal.self`）—— 只是以前藏在 tooltip 裡、不能排序。所以現在把它做成 chip 加排序，資質退成旁邊的比值。

**分子一定要把「練得起來的東西」全部規範化掉**

以前 `self` 是 `{...m, level: 60}`：等級規範化了，**緞帶與主技能等級卻沿用牠現在的值**，而分母（理想個體）是滿的。於是同一份資質會**因為「還沒練」而顯示低分**：

| 物種 | 緞帶0・技Lv1 | 緞帶4・技能滿級 | 差 |
|---|---|---|---|
| 妙蛙花 | 51% | 78% | **+27** |
| 雷丘 | 54% | 76% | **+22** |
| 耿鬼 | 57% | 62% | +5 |
| 哥達鴨／呆呆王 | 76% / 63% | 77% / 63% | +1 / 0 |

（性格／副技能／食材組合完全相同，只差那兩樣。）這是一個**會自我實現的惡性循環**：越沒練的顯示越低分 → 越不想練。規範化之後，資質裡只剩**改不掉的東西**：性格、副技能、食材組合。第 11L 節直接斷言「同一份資質、不同緞帶與技能等級 → 同一個 %」。

代價是 `idealKey` 的簽章要跟著把緞帶正規化掉（只差緞帶的兩隻共用一格快取），但**主技能等級不能正規化** —— `skillNow` 就是看它。

**技能成長刻意不用各專長的主指標**

技能型的主指標是**發動次數**，而發動次數幾乎不隨技能等級變（活力填充S 那幾隻是例外，牠們靠自回活力間接加快幫忙）—— 用主指標去量，最該吃技能糖果的技能型會顯示 0。所以這一項用 `total`（樹果＋食材＋技能能量）。

⚠ `total` 裡的食材是**未經料理加成**的原始能量（料理會再放大約 2.4 倍），所以食材型在這個軸上被低估。**tooltip 一定要寫出來** —— 換算率是憑空的判斷，寧可標明低估也不要編一個係數（和「技能型只看發動頻率」同一條理由）。

**基準等級為什麼固定在 `IDEAL_LEVEL = 60`**

以前是跟著當前等級走，理由是「拿 Lv30 去比 Lv60 的理想值，量到的是還沒練滿」。但那讓數字變成**會自己往下掉的**：第 3 格副技能 Lv50 解鎖、第 3 格食材 Lv60 解鎖 —— 那一格是好是壞，會在跨過門檻的**那一刻**才被算進去。而使用者問的是「糖果餵給哪一隻」，那是關於**練滿之後**的問題。

**60 而不是 50**：第 3 格食材在 Lv60 解鎖，用 50 當基準會讓食材型少算整整一格 —— 那正是食材型主指標的來源。第 4／5 格副技能要 Lv70／80，多數箱子到不了，所以不納入。

六件事：

1. **資質是比值，而且只在同物種之間有意義。** 100% 的皮卡丘不會比 70% 的妙蛙花強。`scoreNote()` 和排序時的 `boxCount`（`SORT_WHAT`）都要寫出**每個排序能怎麼讀** —— 三個數字回答三個不同的問題，混著讀就會用錯軸做投資決定。第 11i／11L 節有斷言。
2. **只有一份算式**（`idealPctFrom`）。摺疊列和展開後的產能列共用它 —— 兩份一定會走鐘，而走鐘的那份會靜靜地顯示錯的數字。第 11i 節斷言兩處的字串相同。
3. **背景填算，不要讓 `renderBox` 變慢。** 一隻 `monIdeal` 要跑約 210 次 `monPower`（15~40ms），60 隻就是 1~2 秒的凍結，而 `renderBox` 每改一個欄位就跑一次。所以 `idealFillAsync()`：還沒算的顯示「資質 —」，`setTimeout` 一批算 `IDEAL_CHUNK` 隻，`paintIdealChips(batch)` **只補剛算好的那幾張**（不重畫，才不會把正在編輯的欄位焦點弄掉），進度寫在 `boxCount` 上。**`.mon-idl` 和 `.mon-inv` 兩組 chip 必須一起補** —— 只補一邊，卡片上會同時出現算好的資質和空著的練滿，看起來像壞掉。`idealJob` 是輪次號碼，`renderBox` 一跑就 +1，舊的那輪自己停下來。**只算目前看得到的**（`visibleIdx()`），而且 `#view-box` 隱藏時整輪不開（`showView('box')` 會補開一輪）。
4. **`IDEAL_SORTS`（資質／練滿／技能成長）這三個排序：算完必須整個 `renderBox()`。** 它們的順序就是這些值，只補文字的話順序是錯的。還沒算完的排最後（`?? -1`）。

5. **空著的副技能格要標成下界（`≥`）。** 副技能沒有負值，所以沒填的格子只會讓**分子**變小 —— 顯示的百分比因此是下界，不是估計值。靜靜地把「還沒記」當成「就是沒有」會誤導投資判斷。`idealUnknownSlots()` 算的是「在評價等級下已生效、但還空著」的格數。
6. **`idealKey` 要用規範化過的簽章。** 三個數字都只取決於 `{...m, level: T, ribbon: 4}`，所以 Lv30 和 Lv45 的同一隻、以及只差緞帶的兩隻，都共用同一格快取；不正規化就會各算一次同樣的東西。**但主技能等級不能拿掉** —— `skillNow` 就是看它。

DOM 順序必須是 `.mon-idy` → `.mon-rest` → `.mon-idl` → `.mon-ings`：grid 的自動排版照 DOM 走，而 `.mon-idl`／`.mon-ings` 各自寫死欄號（1／2）。第 11i 節有斷言。

**食材篩選（`fltIng`）是唯一可以跨專長排的「產量」名次。** 同一組食材、同一個單位，所以「誰產最多品鮮蘑菇」是有意義的問題，答案也可能是一隻全能型。（「技能成長」也可以跨專長排，理由不同：技能糖果是同一種資源。「資質%」則**不行** —— 它是比值，而且只在同物種內有意義。）

**它是攤平的可複選 chips，不是下拉。** 食譜要的是**一組**食材（通常 3 種），下拉一次只能選一個，問不出「誰供得起這道菜」。`boxFlt.ing` 是 `Set<食材索引>`，`boxFlt.ingMode` 是 `'any'`（聯集：供得起其中一味）或 `'all'`（交集：一隻抵好幾隻）。

四條規則：

1. **篩選看的是「食材欄位」，排名看的是「實際產量」。** 食材磁鐵那類主技能會把食材灑遍 `MAGNET_POOL`（除了尾巴以外全部），按產量篩的話幾乎每一隻都中，篩選就沒用了 —— 欄位才是你能規劃的東西。
2. **未解鎖的欄位也篩得到，但產量是 0**（`.mon-power.zero` 變淡、排最後）。遊戲畫面本來就把它預告出來，藏起來使用者看不出「為什麼牠沒出現」，顯示成正常值又是說謊。
3. **選了食材之後摺疊列的數字要換成「選中那幾種的總產量」**，而且 `fltIng` 的 listener 必須 `renderBox()` 而不是只 `applyBoxFilter()` —— 只切 `hidden` 的話顯示的還是各專長主指標，用「選定食材的產量」排序時順序看起來就像壞掉。
4. **`clearBoxFilter()` 要 `syncIngFilterUI()`**（chips 的 `aria-pressed` 是畫出來的狀態，不是真實來源）。真實來源永遠是 `boxFlt.ing`。

`monPower` 是**純函式**：只呼叫 `baseStats` / `memberOutput`，不碰 `POOL`、不需要參考隊。前一版（邊際貢獻）要 `buildPool(參考條件)` 再還原，那個坑現在不存在了。理想值一隻約 15ms，`IDEAL_AUTO_MAX = 6` 以上改成按鈕。

### 陷阱：`nick` 是唯一會進 `innerHTML` 的使用者輸入

其他插值全部來自 `game.json`。暱稱不 escape 的話，一個 `"` 就把 `value="…"` 或 `title="…"` 屬性打斷，一個 `<` 就是直接注入標記 —— 而暱稱會經由 Sheet 同步到別的裝置。所以 `esc()` 用在**三個**地方：摺疊列的文字、`title` 屬性、以及編輯區 input 的 `value`。

`NICK_MAX = 24`，`deserialize` 會把非字串正規化成字串再截斷（舊資料沒有這個欄位，而數字暱稱會變成 `number`）。編輯用的下拉每個都要 ~150px，全部攤開必然佔版面，摺疊是唯一同時滿足「看得完」和「掃得快」的做法。

摺疊列的**副技能底色就是稀有度**（`game.json` 的 `subskills[].r`：gold／silver／white），不是自己配的色。未解鎖的欄位變淡但仍然顯示 —— 遊戲畫面上看得到（🔒Lv.70），先記下來是對的。

**食材是「[食材名 選單] ×N（靜態文字）」，不是兩個選單。** 同一格裡食材種類不會重複（`[null,0]` 空欄位除外），所以**數量由食材決定** —— 選好食材後數量只有一個可能值。做成兩個選單會假裝有不存在的彈性。

**`monCard` 同時給寶可夢箱和截圖校對區用**（校對區傳 `idx == null`：沒有摺疊列、永遠展開）。兩邊各寫一份的話，校對區看到的就不是進箱子之後的東西。

**`clearBoxFilter()` 只清篩選，刻意不動排序。** 以前它連排序一起清回「加入順序」，註解寫的理由是「否則新增的那隻會跑到中間去」—— 但那是把兩件事混在一起：**篩選會讓新增的那隻完全不出現**（預設的皮卡丘常常不符合條件），而**排序只是換位置**，換位置由 `addBtn` 的置頂（見下）＋ `scrollIntoView(data-i)` 解決。實際踩過（2026-09-08）：用「圖鑑編號」在看箱子，按一下「新增一隻」整個列表跳回加入順序，看起來像排序自己壞掉。`addBtn` / 截圖存入 / JSON 匯入三條路徑都走這個函式，所以三條都中。第 11d 節有斷言。

代價是**測試不能再靠 `clearBoxFilter()` 把排序歸零** —— 要歸零就自己設 `fltSort`（第 11e 節開頭就是這樣做）。

### 「新增一隻」暫時置頂（`boxNew`）

「新增一隻」的按鈕在篩選列上（＝畫面最上面），但 `roster.push` 讓新的那隻排在**最後** —— 60 隻的箱子就得往下拉到底才找得到那張要填的表單，填完再拉回來按下一次。所以 `boxNew` 記住那一隻的**真實 roster 索引**，`boxOrder()` 把它提到最前面。

三條規則：

1. **只在它還展開著的時候置頂**（`isBoxNew()` 同時檢查 `monOpen`）。收起來 ＝ 填完了 → 回到目前排序該有的位置。置頂是「我正在編輯這一隻」的暫時狀態，不是一個新的排序規則。
2. **置頂的那一張要標「剛新增」**（`.mon-new`）。在「加入順序」排序下把最新的一隻提到最前面，不講就是「文案說謊」那類 bug —— 排序的名稱和實際順序不符。
3. **不算數就在 `boxOrder()` 裡就地清成 `null`。** `del` 與整批取代都會 `monOpen.clear()`，那之後 `boxNew` 指到的已經是別隻了；留著就會在使用者下次展開那個索引時冒出一個「剛新增」並把牠置頂。清在 `boxOrder()` 裡而不是散在各個路徑，是因為它宣告在 `app.js` 後段而 `deserialize` 在前段 —— 在 `deserialize` 裡寫 `boxNew = null` 會有 TDZ 風險（見陷阱 1）。

`roster` 的順序完全沒動（那會進 `serialize()`／Sheet），只有渲染順序。截圖存入不走這條 —— 那條留在匯入頁、卡片也不展開。第 11d 節有斷言（置頂、標記、收起後歸位）。

篩選列還有：**專長**與**食材**兩個篩選（食材那個見下方「個體產能」）、**排序**（加入順序／圖鑑編號／產能／選定食材的產量／練滿產能／資質％／技能成長／等級／專長／主技能 —— 定義在 `BOX_SORTS`，同鍵時一律 `|| a-b` 回到加入順序）、**排序方向**（見下）、**展開／收起全部**（只作用在目前篩選看得到的那些 —— 一次攤開 60 隻要建一萬多個 `<option>`）、**重複偵測**。

### 排序方向（`boxFlt.dir` ＋ `#fltDir`）

每個 `BOX_SORTS` 比較器有自己的「正向」，`dir === -1` 就把**整個**比較結果乘上 −1：

```js
idx.sort((a,b)=> ((cmp ? cmp(a,b) : 0) || a-b) * dir)
```

三件事：

1. **連 `|| a-b` 那個 tie-break 也一起反轉**，所以反向出來的就是列表完整倒過來（第 11i 節直接斷言 `reverse()` 相等）。「加入順序」沒有比較器，靠的就是這條。代價是「產能」的**專長分組順序**也會跟著倒 —— 這比「按了反轉但前半段沒動」好，按鈕的 `title` 會寫出來。
2. **方向只寫在按鈕上，`<option>` 裡不准再寫一次。** 以前選項寫的是「等級（高→低）」「產能（專長內高→低）」—— 加了反轉之後那行字有一半的時間是錯的。和「文案不能寫死檔名」同一類：同一件事有兩個真實來源，就一定會有一個在說謊。`SORT_DIR` 是每個排序的方向措辭（`['等級高→低','等級低→高']`），`syncSortDirUI()` 把它畫到按鈕上。
3. **換排序時 `dir` 回到 1。** 「等級低→高」看完換去看「主技能」，繼承一個反向會讓人以為排序壞了；而且每個排序的正向本來就是它最常用的那個方向。

**重複的定義是「每一個欄位都相同」**（`dupKey`：物種／等級／性格／五格副技能／三格食材／技能Lv／緞帶）。同物種同等級但副技能不同是完全合法且常見的，所以只有全部一致才算 —— 那幾乎一定是同一隻的截圖看了兩遍。

**任何欄位改動都會 `renderBox()` 重畫整個列表。** 展開時摺疊列還在上面，而它顯示的正是副技能／食材／等級／性格／⚠重複 —— 只改值不重畫，摘要就會和下面的選單不一致。代價是 `select` 的焦點會掉，但 `change` 是「選完才觸發」，可以接受。截圖校對區的 `renderImpReview()` **也是整個 `#impRow` 重畫**，同一個理由。

**所以測試裡不能跨一次改動沿用同一個元素參考。** 改完之後那個節點已經脫離 DOM，而在脫離的節點上 `dispatchEvent` 不會冒泡到 `#boxList` / `#impRow` 的委派處理器 —— 第二次改動靜靜地不生效，看起來像「程式改不回來」。實際踩過（第 11b 節的「改回正確值 → 校驗碼恢復」紅了很久）。每次都重新 `querySelector`。

### 陷阱：`.mon-edit` 裡的 `flex-basis`

`label.f` 是 flex item，裡面的 `select`／`input` 是 `width:100%`。**如果 label 沒有 `flex-basis`**，那個百分比會回頭對到 `.mon-row`，結果每個欄位都撐成整行寬、一行一個 —— 實際踩過，畫面變成一隻寶可夢佔六行。所以 `.mon-edit .w-sp/.w-nat/.w-num/.w-rib` 都必須給 `flex` 基準值。

### 陷阱：`grid-template-columns:1fr` 縮不到 min-content 以下

`1fr` ＝ `minmax(auto, 1fr)`，而 grid item 的 `min-width` 預設是 `auto` —— 所以那一欄
**縮不到內容的 min-content 以下**，容器不夠寬時 item 就直接溢出去。

**實際踩過（2026-09-10，390px 手機）：** 推演分頁的 `.panel` 被撐成 405.8px（容器只有
350px），`documentElement.scrollWidth 487 vs clientWidth 390` —— **整頁**橫向捲動，
每個數字都要左右拖才看得完。三個地方都中：`#results` 裡兩個 inline 的 `.grid`
（`1.1fr`／`1fr`）與 `.hero`。

修法是 `.grid > *{min-width:0}` ＋ `.hero > *{min-width:0}`，寬內容自己在 `.scroll`
（`overflow-x:auto`）裡橫捲 —— 那本來就是設計。**不要改成 `overflow:hidden`**，那會把
內容裁掉，而這個 repo 的立場是「裁掉等於沒有」。

第 13 節在 390px 下逐分頁斷言 `scrollWidth <= clientWidth`，而且會把**不在捲動容器裡**
的那幾個元素印出來（在 `.scroll` 裡的橫捲是對的，不算）。**新增任何 grid 版面時記得
給 item `min-width:0`。**

### 陷阱：刪除之後的展開狀態

`monOpen` 存的是 roster 索引。`roster.splice(i,1)` 會讓後面的索引整批往前位移，所以 **del 之後一律 `monOpen.clear()`** —— 留著會展開到「原本是下一隻」的那一隻身上。`tests/smoke.mjs` 第 11d 節有斷言。

**`deserialize()` 整批取代 roster 時也一樣要清。**（漏過一次，2026-09-08 補）它把整個 `roster` 換掉，索引指的已經是完全不同的寶可夢 —— 從雲端下載、JSON「取代」匯入都會走到這條，症狀是「開頁面就有一張莫名展開的卡」。`append` 不動舊索引，所以那條路徑不清。

**任何「整批換掉 roster」的新路徑都要想一次這件事。** 它不會壞資料（`data-i` 是重畫時寫的，永遠正確），所以沒有任何錯誤訊息 —— 只有畫面怪。

#### 存 roster 索引的東西**一共有四份**，`lastResults` 是最後被補上的那一份（2026-09-10）

| 誰 | 刪一隻要做什麼 | 整批取代要做什麼 |
|---|---|---|
| `monOpen` | `monOpen.clear()` | `monOpen.clear()` |
| `teams[].members` | `teamsAfterDelete(i)`（前移） | `teamsReset()` |
| `boxNew` | `boxOrder()` 裡就地清 | 同左 |
| **`lastResults[n].idxs`** | **`dropResults()`** | **`dropResults()`** |

`lastResults` 以前完全沒人處理。實際踩過：跑完推演 → 到箱子刪一隻 → 回推演分頁點
「替代隊伍」任一列 → `renderResults` → `memberCard` 讀 `roster[undefined]`，**整頁
TypeError**。而索引剛好還在範圍內的時候更糟：不會報錯，只是靜靜地把**另一隻**的名字
與暱稱顯示在那張成員卡上。

三條規則：

1. **作廢，不要試著修正索引。** 刪一隻之後那份結果的「最佳組合」本來就可能不是最佳了
   （被刪的那隻可能就在裡面），修索引只會產生一份看起來還在、其實已經不成立的答案。
2. **要講出來為什麼不見了**（`resultsStale` → 「箱子改過了…請再按一次」）。靜靜地變回
   「還沒推演」的空狀態，使用者只會以為結果自己壞掉了。自組隊伍分頁的 `#tmNote`
   本來就有這句話，兩邊取同一個旗標。
3. **`renderResults()` 自己也要有 `resultAlive` 的最後防線** —— 任何漏了 `dropResults()`
   的新路徑會在那裡被擋下來，而不是把整頁炸掉。

`append`（JSON 追加、`addBtn`、截圖存入）不動既有索引，所以那三條路徑都不必清。
第 11r 節有斷言。

**食材是「[食材名 選單] ×N（靜態文字）」，不是兩個選單。** 同一格裡食材種類不會重複（`[null,0]` 空欄位除外），所以**數量由食材決定** —— 選好食材後數量只有一個可能值。做成兩個選單會假裝有不存在的彈性。

### 陷阱：`sp` 在序列化格式裡是**內部名**，不是索引

`serialize()` 寫 `sp: D.dex[m.sp].n`（`"VENUSAUR"`），`deserialize` 也照名字找。但**執行時的 `roster` 存的是 dex 索引**，`impSolve` 產出的也是索引 —— 兩種形式長得完全不一樣卻都合法，所以很容易搞混。

**實際踩過（2026-09-08）：** 我把 `impSolve` 的結果直接寫成 JSON 給使用者匯入，`sp` 是索引。舊版 `deserialize` 只做 `findIndex(p => p.n === r.sp)`，數字永遠找不到 → 一律退回 `0`，於是**五隻不同的寶可夢一起變成妙蛙種子**，而畫面上沒有任何提示 —— 等級、性格、副技能都還是對的，所以看起來只是「後面算出來的東西怪怪的」。

修法兩件事都做：`reviveSp` **整數當索引接受**（`impSolve` 的輸出可以直接貼），而真的認不出來就記進 `badSp`，由匯入按鈕**講出來**。索引只在同一份 `game.json` 快照裡穩定，但沒有任何地方會**寫出**索引，所以風險只在貼上那一瞬間。

### 陷阱：`'neutral'` 是 truthy

無修正的性格在 `game.json` 裡是 `p: "neutral"`，**不是空值**。所以 `n.p ? NAT_AB[n.p] : '無修正'` 會走進 true 分支，`NAT_AB['neutral']` 是 `undefined` —— 畫面變成「害羞 **+undefined −undefined**」。25 種性格裡有 5 種中（害羞／勤奮／坦率／浮躁／認真），而且性格選單（`natLabel`）和摺疊列（`natBrief`）**兩處都中**。

所以統一走 `natMod(n)`：只有 `n.p && n.p !== 'neutral'` 才回 `{up, dn}`。`natAb` 對照表查不到的鍵回傳原始鍵，也不會變 undefined。

### 陷阱：刪除沒有 undo，而且圖示不能和可逆的按鈕撞在一起

刪除就排在三顆隨手切換的按鈕旁邊，手滑一格就少一隻，而 `save()` 是即時的、雲端馬上跟著覆蓋。所以 `del` 一定要 `confirm`，而且**訊息裡要寫出是哪一隻**（`#圖鑑號 名稱 Lv 性格`）—— 排序或篩選過的列表上根本分不出按到誰。＋隊／📌／✕（排除）反過來**不該**問（都可逆）。

**圖示改過一次（2026-09-09，使用者反映）：**

| | 原本 | 現在 | 為什麼 |
|---|---|---|---|
| 從推演中排除 | `○` ／ 排除中 `🚫` | `✕` ／ 排除中 `🚫` | **`○` 在中文慣例裡是「可以」**，掛在一顆叫「排除」的按鈕上意思正好相反 |
| 刪除 | `✕` | `🗑` | `✕` 讓給排除那顆之後就不能再用；而且**不可逆的動作不該和可逆的長得一樣**（`.btn.danger` 平常還會淡化，hover 才轉紅） |

**兩顆按鈕長一樣，就是在請人按錯。** 這一條和「刪除一定要 `confirm`」是同一件事的兩層防線 —— 少了圖示這一層，confirm 就得天天擋。

### 寶可夢箱的「＋隊」（`teamAddFromBox`）

把箱子裡的一隻直接放進**目前顯示的那一支**自組隊伍（`teamShown`）的第一個空格。找寶可夢的地方本來就是箱子（有篩選、排序、資質%），所以「看到就順手放進去」比「切到自組隊伍 → 開選擇器 → 再搜尋一次」少三步。

四條規則：

1. **目標一定是 `teamShown`，不是「隨便找一支空的」。** 和 `teamFromResult`（整組複製 → 該開新的一支）方向相反：這個是逐隻放，一隻進 A、下一隻跳到 B 的話根本組不起來。**按鈕的 `title` 要寫出是哪一支** —— 畫面上同時有 4 支的可能，不講就是猜的。
2. **同隊不可重複，跨隊可以** —— 和選擇器同一條規則（同一隻放兩次會讓 Helper Boost 的物種計數、流星群的龍屬性種類數全部算錯）。所以按鈕狀態存的是**列表**（`inTeam`）不是布林。
3. **已經在裡面／已經滿 5 隻，都要出聲**（`setStatus`）。靜靜地什麼都沒發生是最糟的回應 —— 使用者只會再按幾次。訊息裡要有暱稱**與學名**（`monLabel`），箱子裡有兩隻妙蛙花時分不出加到的是哪一隻。
4. **在箱子裡就看得出牠在哪幾支隊伍裡**（`.btn.on` 點亮 ＋ `title` 列出隊號）。按下去有沒有生效，不該要切分頁才知道。

自組隊伍不進 `serialize()`，所以這條路徑**不 `save()`**，只 `renderBox()` 重畫按鈕狀態。

測試裡不要用原生對話框驅動這條路徑：`confirm()` 會擋住 renderer，在 `page.evaluate` 裡容易卡死。第 11d 節是在頁面內把 `window.confirm` 換掉，取消與確定兩條都測。

### 陷阱：篩選後的 `data-i`

`applyBoxFilter` 用 `hidden` 切換而不是重建列表（一張卡有 246 個種類選項，60 隻就是一萬多個 `<option>`，每次打字重建會卡）。所以 **`data-i` 一律是真實的 roster 索引**。

用「篩選後的序號」當索引的話，改一格會改到別隻身上，**而且不會有任何錯誤訊息** —— 只是資料悄悄錯掉。`tests/smoke.mjs` 第 11d 節會刻意讓「篩選後的位置」和「真實索引」不一致，然後斷言改動落在正確的那一隻。

## 雙後端持久層

一份程式碼，執行時自己判斷：

```
window.claude 存在  → claude.use('db') → artifact 資料庫（doc: box/main）
否則 + 已設定同步    → 使用者的 Google Sheet（Apps Script Web App）
否則                → 只有 localStorage
```

`localStorage['psleep-box']` **任何情況下都會寫**，當離線副本。

**存進去的不只是寶可夢箱。** `serialize()` 的 payload 是 `{roster, wk}` —— roster 的每一筆包含 `nick`（自訂暱稱），而 `wk` 裡有 `recipeLevels`（78 道食譜的個別等級）—— 所以三個後端都會一起同步。唯一每台裝置各自存的是 Apps Script 的**網址與金鑰**（`psleep-sync-url` / `psleep-sync-token`），以及純檢視狀態（`monOpen` 展開、`boxFlt` 篩選排序），那些刻意不進 `serialize()`。

原本同步面板標題、面板說明、版本面板三處都只寫「寶可夢箱」，而食譜等級那一頁根本沒講 —— 使用者因此以為食譜等級是本機的、換裝置要重填 78 道（實際踩過，直接來問）。所以那句話集中在 `SYNCED_WHAT` ＋ `storageWhere()` ＋ `renderStorageNote()`，四個地方都取同一份，跟 `PATHS` 一樣的道理。

`storageWhere()` **看的是 `backend`（`'artifact'|'sheet'|'local'`），不是 `window.claude` 或 `sync.on`**。後兩者只代表「有設定」；連不上的時候推斷出來的答案是假的，而這句話正是使用者用來決定「要不要設定同步」的依據。`backend` 只在真的往返成功之後才改。

### 一次性設定連結（`#sync=…&token=…`）

換裝置時那組 Apps Script 的 `.../exec` 網址長得沒辦法用手打，所以「複製同步連結」把網址與金鑰包成一條網址：在新裝置上開那條連結就等於填好了兩個欄位。`buildSyncLink()` / `parseSyncLink()` / `stripSyncLink()` / `applySyncLink()` 在 `app.js` 的 Sheet 後端那一區。

四條規則：

1. **參數放 hash（`#`）不放 query（`?`）。** 金鑰是這個後端唯一的憑證，而 hash 不會送到伺服器 —— query 會進 GitHub Pages（或任何靜態主機）的存取記錄。讀取端為了容錯**兩種都吃**，但 `buildSyncLink()` 產出的一律是 hash。
2. **讀完立刻 `history.replaceState` 把參數拿掉。** 金鑰不該留在網址列上被截圖、被複製、或留在瀏覽器歷史裡。`stripSyncLink()` **只清 `sync` / `token` 兩個鍵** —— 沒有我們的鍵就整段原封不動，否則 `URLSearchParams` 會把 `#view-box` 那種片段重寫成 `view-box=`。
3. **和現有設定不同就不自動套用。** 一條連結能改掉資料的目的地，套用之後本機的改動會開始往別人的 Sheet 上傳、自己那份停在舊版 —— 和「刪除沒有 undo」同一類的不可逆。所以只有**本機還沒設定**（＝換裝置的實際情境，沒有東西會被蓋掉）或**和現在完全相同**才直接套用；不同就只填欄位、顯示 `.notice.warn` 並寫出連結指向的 host，要使用者自己按「連線並下載」。
4. **artifact 版本不套用但還是要 `stripSyncLink()`。** 那邊用的是 artifact 自己的資料庫，Sheet 設定根本不生效 —— 但金鑰不能因此留在網址列上。

`tests/smoke.mjs` 第 8b 節（19 項）測這四條。**測試裡的 `openWith()` 每次都帶一個不相干的 `?t=N`** —— 只改 hash 的 `page.goto` 不會真的重新載入（同一份文件的片段跳轉），`boot()` 就不會再跑，整節會假通過。那個 `?t=N` 順便驗「不相干的 query 要留著」。

面板文案也要跟著講（「複製同步連結」怎麼用 ＋ **那條連結等於金鑰本身**），第 8b 節有斷言 —— 少了那句就是「文案說謊」那類 bug 的反面：功能存在但沒人知道。

`!window.claude` 就是「自架版本」的判斷依據 —— 用來顯示同步面板、停用「請求更新資料」按鈕、以及版本面板的標示。**新增環境相依的文案時記得兩邊都要對。**

Google Sheet 後端在 `apps-script/Code.gs`，設定步驟見 `SETUP-google-sheet.md`。

## 已經踩過的陷阱 — 不要再踩

### 1. `$` 的 TDZ

`const $ = id => document.getElementById(id)` **必須留在 `app.js` 的最前面**。持久層在頂層就會呼叫 `$('syncPull')`，`const` 不會提升，宣告放後面 → 整支腳本在載入時 TDZ 錯誤、**整頁死掉**。

搬動任何頂層區塊時，先確認相依順序。

### 2. Apps Script 的 CORS preflight

上傳一定要用 `Content-Type: text/plain`。用 `application/json` 會觸發 preflight `OPTIONS`，Apps Script 不回應 OPTIONS，直接失敗。後端自己 `JSON.parse(e.postData.contents)`。

### 3. 貪婪排程不單調

`mealPlan` 單純依 `rv` 由高到低貪婪**不是單調的** —— 調高某道食譜的等級可能讓它選錯開場、總分反而變低。`bestPlan` 用多重起點（`null` ＋ `rankSingle` 前 8 名）繞過。

**任何改動 `mealPlan` / `bestPlan` 的人，必須跑單調性測試**（`tests/smoke.mjs` 第 3 節）。

**「讓決賽變強」也會破壞單調性 —— 這不直覺，實際踩過（2026-09-09）。** 把 `bestPlan` 的開場從 9 個改成「每一道煮得出來的都試」（料理分數 +0.9%，41.7% 的隊伍受惠），結果 40 組隨機食譜等級裡有 5 組**下降**，最多 −1.4%。

原因不在 `bestPlan`（它取 max，開場越多分數只會越高），而在**它與搜尋階段的落差**：搜尋用 `mealPlan` 單起點（下界），決賽變強之後兩者的排名落差變大（搜尋分數/真值的中位數 1.0000 → 0.9974），真正最佳的隊伍會因為搜尋分數低而擠不進 `FINALISTS`，而「哪一組擠不進去」會隨食譜等級跳動。已還原，細節與三個修法方向見 `TODO.md` 第 10c 項。

**推論：搜尋階段的估計與決賽的估計，落差越小越安全。** 只改其中一邊（不管哪一邊變準）都可能讓排名不穩。

### 4. 候選篩選只能是「產品規則」，不能是「效能手段」

`wk.strictBerry`（預設開）：樹果型必須產本週加成樹果才進候選。**這是使用者要的需求**，不是最佳化 —— 實測會讓總能量低 3.9%~6.6%，那是刻意的取捨。細節與量測見 `DECISIONS.md`。

三件事不要動：
- **`wk.fav` 為空時規則不生效** —— 否則會把所有樹果型都排除
- **📌 固定的成員不受此限** —— 使用者的個別指定優先於通則
- **排除名單要顯示出來**（`comboCount` 的文字與 `title`）—— 靜靜地少算候選就是「文案說謊」那類 bug

**絕對不要為了加速而加新的候選篩選。** 這個 repo 已經因此踩過兩次：一次是按 roster 順序的預篩（結果隨箱子順序改變，差 16.34%，已移除），一次是「用 `specialty` 猜價值」（`specialty` 是分類標籤，不是價值來源 —— 樹果型可能因為主技能而有價值）。要壓縮搜尋空間就做**可證明的 branch-and-bound**（樂觀上界剪枝），見 `TODO.md`。

### 5. 搜尋目標與評分目標必須一致，而且**搜尋分數只能是下界**

搜尋階段用 `mealPlan` 單起點（貪婪、會扣除食材），決賽用 `bestPlan`（多起點）。兩者若脫鉤，最佳隊伍會被擠出決賽名單。

**這裡曾經用 `proxyDish`（樂觀上界），而那是錯的（2026-09-09 修正）。** 它走訪食譜填滿 21 餐但**不扣除食材** —— 同一批蘋果被每一道用到蘋果的食譜重複計算。註解寫著「ranks teams the same way the real plan does」，實測完全不成立：

| | 中位數 | 最高 | 搜尋選出的隊伍 | 真實前 8 名進不了決賽 |
|---|---|---|---|---|
| `proxyDish` / 真值 | **1.389** | 2.85 | 比最佳**低 9.2%** | **8 組全部**（FINALISTS 放到 1000 還有 5 組） |
| `mealPlan` 單起點 / 真值 | 1.0000 | 1.0000 | **0.00%** | 0 組 |

高估的**幅度隨隊伍的食材種類分布而變**，所以上界連排序都不保 —— 這是關鍵：一個一致的高估倍率不會影響排名，但這個不是。

**搜尋分數必須 ≤ 真實排程**，理由是 `finalizeTeams` 的 `if (mp.total > b.dishS)` 靠它才會生效。用上界的話那行幾乎永遠不成立，決賽算了真實排程卻沒用它，於是 UI 的「料理」數字和 21 餐排程表的小計對不上（實測差 58%）。實測 **47.7%** 的隊伍多起點比單起點好 —— 那些就是靠這行修正回來的。

`scoreTeam` 的**兩個分支都要跑排程**：`manual`（指定食譜）以前只算指定那一道，但決賽的 `bestPlan(forced)` 會用別的食譜填滿剩下的餐 —— 低估中位數 26%，真實前 8 名有 5 組擠不進決賽。同一個病。

成本：33,649 組多花 466ms（單執行緒 550ms → 1400ms），只有 `bestPlan` 的 0.11x。**不要為了省這個時間再加任何「不扣除食材」的近似** —— 料理分數的本質就是 `cooks = min(floor(pool[i]/a))` 的木桶效應，忽略它等於忽略整個問題。第 2b 節直接斷言「搜尋分數是下界」，任何上界近似加回來都會紅。

`FINALISTS = 50`（進決賽）、`SHOWN = 8`（顯示）。**改動評分方式時要重新確認 `FINALISTS` 夠不夠大。**

### 6b. 隊伍搭配技能：Helper Boost 是**每隻各自一列**，不是整隊一個純量

`HB_TABLE` 的列 ＝ 隊上與**持有者自己的樹果**相同的**不同物種數**（含牠自己，1~5），欄 ＝ 技能等級。三隻神獸的樹果各不相同（雷公 GREPA／炎帝 LEPPA／水君 ORAN），所以 `ctx.hbRows` 是 `{樹果: 列數}` 的 map。

**踩過（2026-09-08）：** 原本是 `idxs.find(...)` 取「第一個」持有者算出一個純量 `hbU`，整隊共用。兩隻神獸同隊時另一隻會被套上別人的列，而 `find` 取的是 **roster 索引最小**的那隻 —— 於是**同一支隊伍只要換 roster 順序，答案就會變**（實測 `helpsGiven` 158.75/97.45 ⇄ 84.17/203.93，差 2.3 倍）。那違反第 4 節的順序不變量，但第 4 節的抽樣（每 9 隻取 1）抽不到兩隻神獸 —— 整個 dex 只有 3 隻，而且圖鑑號連號。第 5 節現在有斷言。

`hbKey()` 把 map 排序後序列化進 `ctxKey`，見下一條。單一持有者的隊伍數值**完全沒變**（175.51／193.66 前後一致）。

**Plus / Minus 互為條件。** 兩邊都只有在隊上有另一半時才給**加成**；基礎效果（正電的 `ingredient`、負電的 `potSize`）沒有條件，照給。`ctx.hasPlus` / `ctx.hasMinus` 各 gate 一側，**兩個都在 `ctxKey` 裡**。

**踩過（2026-09-08）：** 正電那一側原本是在 `rawPayload` 裡把 `bonusIngredient` **無條件加一半**（`/2`）折進 `ingSpread`，`ctx` 連 `hasMinus` 都沒有。後果是單獨帶正電被**高估**、正負配對被**低估** —— 搜尋會系統性地錯過那個配對。改成獨立的 `pay.ingBonus`，由 `memberOutput` 依 `ctx.hasMinus` 決定加不加（實測配對 137.8 vs 單獨 91.5，1.51 倍）。第 5b 節有斷言。

**第三組：拉帝亞斯／拉帝歐斯。** 資料裡的欄位名稱本身就說明了它是條件式的。
遊戲技能頁（2026-09-09 由使用者提供）證實了兩件事：

- 拉帝亞斯「治癒波動（活力療癒S）」的**額外幫忙**是 `helps`（基礎）＋ `latiosHelps`（隊上有拉帝歐斯時的加碼），
  技能頁直接寫「基礎 + 額外 = 總計」，而 `helps + latiosHelps` 逐級等於那個總計欄
  （1+1=2、2+1=3、2+2=4、3+2=5、4+2=6、4+3=7）—— **是加碼，不是取代**。
  語意和幫手支援S 的 `help` 相同（給一隻隊友），所以走同一個 `helpsOne`。
- 拉帝歐斯「流星群（樹果遽增）」的 `latiasBerries` 是**另一張表**（「樹果遽增(根據寶可夢)」），
  和基礎的「樹果遽增(根據種族)」並存。

**踩過（2026-09-09）：** `rawPayload` 以前把 `latiasBerries` **無條件**寫成 `o.selfBerry`
（拉帝歐斯單獨上場也照領，技能Lv6 時佔牠總產能 **29.5%**），而拉帝亞斯的 `helps` / `latiosHelps`
**完全沒讀**（`helpsGiven` 是 0）。現在兩邊都由 `ctx.hasLatias` / `ctx.hasLatios` 決定，
**兩個都在 `ctxKey` 裡**。實測：拉帝亞斯每日幫忙 18.34 → 30.17，拉帝歐斯樹果能量 9617.5 → 12358.9。

**治癒波動一次打 2 隻。** 遊戲內說明（主技能Lv.1）：「隨機讓隊伍的 **2 隻**寶可夢回復活力 6，
並讓牠們立刻完成 1 次幫忙。隊伍中有拉帝歐斯時，還會讓牠們再立刻完成 1 次幫忙。」
所以 `energy` / `helps` / `latiosHelps` 都是**每一隻**的量，要乘上 `HEAL_PULSE_TARGETS = 2`
（一般的活力療癒S 是 1 隻）。以前一律當 1 隻，治癒波動因此被低估一半。
實測拉帝亞斯每日幫忙 18.34 → 38.37（單獨）、30.17 → 70.63（有拉帝歐斯）。
⚠ 目標數只在 Lv.1 的說明裡看到，技能頁的資料表沒有這一欄，所以**推測不隨等級變**。

**拉帝歐斯的流星群有兩份，都算了：**

1. **基礎表** —— 隊上**不同種類的龍屬性**幫手數（含牠自己，1~5）決定自己與隊友各拿幾顆樹果。
   遊戲內說明：「隊伍中有越多不同種類的龍屬性幫手寶可夢，樹果數量就會增加得越多。」
   條件是**龍屬性**，不是樹果（一度以為和 Helper Boost 一樣看樹果，錯了）。
   `ctx.nDragon` 在 `teamContext` 算好、進 `ctxKey`。
2. **`latiasBerries`** —— 隊中有拉帝亞斯時，**自己**再 +N（上游快照有這一欄）。

**那張基礎表上游快照完全沒有**，來自遊戲技能頁的截圖（2026-09-09），存在
`tools/skills-extra.json` → `data/game.json` 的 `msExtra`。外層 index ＝ 主技能等級 1~6，
內層 ＝ 龍屬性種類數 1~5：

| 種類數 | Lv1 | Lv2 | Lv3 | Lv4 | Lv5 | Lv6 |
|---|---|---|---|---|---|---|
| 1 | 12/1 | 21/1 | 29/1 | 38/1 | 43/2 | 48/3 |
| 5 | 20/2 | 33/2 | 41/3 | 49/4 | 53/5 | 58/5 |

（自身/隊友，中間三欄見資料檔。）第 5c 節**逐格斷言**這張表 —— 這是目前少數有絕對數值的斷言之一。

**這三組是這份快照裡僅有的「**發不發**要看隊友是誰」的機制。** 掃過 36 種主技能：其他會影響隊友的
（活力全體療癒／活力鼓舞／樹果遽增的 `teamBerry`／幫手支援）都是**無條件發送**，不挑夥伴；
再加上兩個副技能 `Helping Bonus`（`nHB`）與 `Energy Recovery Bonus`（`nERB`）。
加新的之前先確認它到底是「條件式」還是「無條件發送」。

**但「無條件發送」不等於「隊友是誰不影響數值」** —— 見下一節。

### 6h. 發給隊友的樹果，算的是**隊友自己的**樹果（2026-09-10）

樹果遽增（Berry Burst）與流星群（Draco Meteor）的 `teamBerry` 是**無條件發送**的，
但**發出去的是隊友會撿的樹果**。遊戲內說明：

> 獲得自己**以及隊伍中的寶可夢會撿來的**樹果。

**踩過：** `memberOutput` 以前寫的是 `sim.procs * pay.teamBerry * 4 * bp * favMul` ——
四個隊友全部套上**持有者的樹果**，而且套上**持有者的加成倍率**。實測（Treecko 帶
DURIN，隊友 GREPA／DURIN／LEPPA／ORAN）：本週加成只設 DURIN 一種時，牠的
`berryStrength` 從 14,163.7 跳到 28,327.3 —— **整整 ×2**，連三隻根本不產加成樹果的
隊友那一份也跟著翻倍。Lv6 的 `teamBerry` 是 5 顆 ×4 人 ＝ 20 顆，對照 `selfBerry`
30 顆，所以那是這個技能約**四成**的產出，方向可高可低（隊友的樹果比持有者貴就變低估）。

現在走 `ctx.mateBerryPow` ＝ 整隊五隻的 `berryPower(牠的樹果, 牠的等級) × 牠自己的加成倍率`
總和，再扣掉自己那一份。四件事：

1. **它刻意`不`進 `ctxKey`，而是在記憶化之外事後補。** ⚠ 這一條 2026-09-10 當天
   翻過一次：第一版把 `mateBerryPow` 放進 `ctxKey`（理由是「隊友是誰會改變答案」，
   那個理由本身沒錯），結果是**使用者 89 隻的箱子跑到 25% 分頁 OOM 崩掉** ——
   那是個連續值，快取因此跟組合數線性成長。完整量測與判準見陷阱 6。
   現在 `memberOutput` 回傳 `mateBerryCoef` / `ownBerryPow`，`scoreTeam` 用
   `mateBerryAdd()` 補上，數值**逐位相同**。
2. **`teamContext` 仍然只在隊上有持有者時才算**（`idxs.some(i => givesTeamBerry(...))`），
   但那現在只是省掉 5 次 `berryPower`，**和快取粒度無關**（那條線在 `ctxKey` 上）。
   `givesTeamBerry` 要涵蓋 `D.ms[ms].teamBerry`、`Draco Meteor`
   （牠的 `teamBerryByDragon` 是 `memberOutput` 才疊上去的，`rawPayload` 看不到）
   以及 `WILDCARD`（平均值裡含一份 `teamBerry`）。
3. **單獨一隻（`SCORE_CTX`）這一份就是 0**，那是對的 —— 沒有隊友可發。
   但**量不到就要標出來**：`monPower` 的 `teamOnly` 要加「發給隊友的樹果」。
4. 這是**每週加成樹果 × 隊伍組成**的交互作用，所以「已知簡化」那一段要寫出來
   （使用者會拿它決定要不要把某隻放進去）。

第 5d 節有斷言：加成只中持有者那一種樹果時**整份產出不可以直接翻倍**，換掉隊友的
樹果組成產出要跟著變，`mateBerryPow` **不可以**進 `ctxKey`，共用 memo 與每隊一份新
memo 的結果要完全相同（沒有跨隊串味），箱子要標「隊伍型」。**斷言要走 `scoreTeam`
而不是 `memberOutput`** —— 後者本來就不含隊友那一份。

**`monPower` 的 `teamOnly` 要涵蓋這四類**（幫忙加成／幫手加速／正電負電／發給隊友的樹果），回傳的是原因字串。`SCORE_CTX` 是單獨一隻，這幾個的價值在那個情境下都量不到 —— 量不到就要標出來，不能假裝算進去了。

### 6c. 夢魘（達克萊伊）：扣活力 ＋ 一份 repo 自己維護的惡屬性清單

遊戲的技能說明：「卡比獸的能量增加 (#1)，且讓幫手隊伍中**惡屬性以外**的寶可夢活力下降(#)。」
加成那一欄隨等級（＝快照裡的 `strength`，2640→18515），**扣活力是固定 12，不隨等級變**，
而且快照裡**沒有這一欄** —— 所以 `BAD_DREAMS_DRAIN = 12` 是引擎裡的具名常數，註解引了說明原文。

**扣活力不能併進 `ctx.supportEnergy`。** 那是「每個人都拿到一樣多」的量，而夢魘只打
**惡屬性以外**的成員（惡屬性隊友與達克萊伊自己免疫）。所以另外走 `ctx.darkDrain`，
在 `simulate` 裡由 `bs.dark` 決定要不要吃 —— 逐一判斷，不是整隊開關。
`darkDrain` **也要進 `ctxKey`**（陷阱 6）。

實測（同一支隊伍，只把扣活力開／關）：

| 隊伍 | 週能量變化 |
|---|---|
| 達克萊伊 ＋ 4 隻非惡屬性 | **−7.3%** |
| 達克萊伊 ＋ 2 惡 2 非惡 | −4.3% |
| 達克萊伊 ＋ 4 隻惡屬性 | **0.0%** |
| 沒有達克萊伊 | 0.0%（必須完全相同） |

**`tools/types.txt` 是 repo 自己維護的資料，和 `tools/zh.txt` 同一個性質。**
上游萃取出來的 dex **沒有屬性欄位**（只有 `n d no sp f ip sk b cs pe re ms i0 i30 i60`），
所以屬性清單（惡 13 隻、龍 16 隻）是本專案維護的，分區格式 `## DARK` / `## DRAGON`。三件事：

1. **重建時不能弄丟。** `tools/extract-data.mjs` 會合併它，而且**名字對不上 dex 就直接丟例外** ——
   錯的名字不會有任何症狀，只會讓那一隻被夢魘白扣一次（或在流星群的種類數上少算）。
2. **`meta.schema` 已經因此 +3（1 → 4）。** 新增 `dark[]`（2）、併成 `types{dark,dragon}`（3）、
   再加 `msExtra{}`（4）都是結構變更；不擋的話
   「新 app.js ＋ 舊 game.json」會讓扣活力靜靜消失。`SCHEMA`／`meta.schema`／萃取腳本三處都改了，`index.html` 載入器的必要鍵也要跟著換。
3. **惡屬性的寶可夢在箱子裡會標「惡」**，清單因此是可以被使用者核對的。
   `MS_CAVEAT` 裡夢魘的 `dir` 是 `'data'`（不是 `over`／`under`）——
   效果有算，但它吃一份 repo 維護的資料，那份錯了分數就會偏。

其他 `MS_CAVEAT` 的 `under`（會低估）維持不變：月光的暴擊、怪力鉗的暴擊食材、
治癒波動的額外幫手、蹭蹭臉頰的技能幫手，以及三個「夢之碎片不計分」。
**目前沒有任何 `over`（會高估）的項目了。**

### 6g. 活力的衰減與回復：**模型是對的，不要再「修」它**（2026-09-10 查證）

會有人（我就是）看到下面這個結果就以為引擎壞了：

> 一支**完全沒有補師**的隊伍、**睡滿 8.5 小時**，每天結束時活力是 **0**
> （起床100 → 睡前19 → 睡後0，一天只有 15% 的時間在活力 80 以上）。

算術上確實是結構性赤字：**睡眠回復上限 100，但一天掉 144**（24h × 每 10 分 −1），
三餐只補約 +18（`mealRecovery` 在活力高時一餐只給 1）。淨 **−26／日**。

**但這就是遊戲的設計。** 查證來源：日文驗證 wiki [げんき](https://wikiwiki.jp/poke_sleep/%E3%81%92%E3%82%93%E3%81%8D)

| 項目 | 遊戲 | 引擎 |
|---|---|---|
| 衰減 | 10 分鐘 −1，**睡眠計測中照樣減少** | `e - 1`，日／夜兩個迴圈都跑 ✓ |
| 睡眠回復 | 睡眠分數 1 分 ＝ 活力 1，8.5 小時滿分 | `sleepMin*(100/510)` ✓ |
| 回復上限 | 幫手寶可夢 **100** | `cap = 100` ✓ |
| 主技能可推到 | **150** | `Math.min(150, …)` ✓ |

⚠ **關鍵的一句**（wiki 原文）：
「※Ver.1.0.16までは睡眠計測中は減少しなかったが、**これは不具合であったため**、
Ver.1.1.0にて修正された。」
—— **「睡覺時不掉活力」正是官方修掉的一個 bug。** 想「修」成那樣的人，是在把
1.0.16 的 bug 寫回來。實測那樣改會讓一般隊伍的幫忙次數憑空 +24%。

這也解釋了為什麼活力全體療癒S 那一批（胖可丁、幸福蛋、沙奈朵、克雷色利亞）在
遊戲裡是頂級位置 —— **模型和實際 meta 是一致的**，那本身就是一個佐證。

**附帶的真實結論**（使用者 2026-09-10 問「胖可丁自己居然沒辦法保持活力」）：
補師發 N 給**每個人**，自己也只拿 N；帶活力填充S 的隊友則是 N **再加**自己那一份。
所以睡眠不足時，**純補師（沒有自回、又不是惡屬性免疫夢魘）會是全隊最先垮的那個**。
實測同一支隊伍：睡 8.5h 時牠 100%，睡 6.5h 時 0%。這是對的，不要去「修」。

**唯一還沒查到來源的**：`cap = bs.hasERB ? 105 : 100` 裡那個 105（副技能
Energy Recovery Bonus 是否把回復上限也一起抬高）。wiki 只寫了幫手上限 100。
動到活力回復時順便查一下。

### 6f. 主技能「回自己活力」只有牠自己拿得到（2026-09-10）

和 6c（夢魘扣活力）是**完全同一條規則**，方向相反：

> **只有某些人拿到（或被扣）的量，不可以走 `ctx.supportEnergy` 那條共用管道。**
> 那條管道的語意是「隊上每個人都拿到一樣多」。

**踩過：** `memberOutput` 以前把 `pay.energySelf` 併進 `energyGiven`，而 `teamContext` 再 `energy/5` 攤給全隊 —— **持有者只拿到 1/5，另外四隻拿到牠們根本不該有的活力**。

因為 `energyF` 是**階梯函數**（80以上 ×0.45 → 0 ×1.00），「集中給一個人」和「攤平給五個人」差非常多。實測（單獨一隻，Lv60、技能滿級、緞帶4）：

| 物種 | 專長 | 自回活力／日 | 幫忙次數（錯的 → 對的） |
|---|---|---|---|
| 大食花 | 食材 | 99 | 59.2 → 77.8 **(+31%)** |
| 拉達 | 樹果 | 76 | 56.2 → 73.8 **(+31%)** |
| 嘎啦嘎啦 | 樹果 | 96 | 50.3 → 66.0 **(+31%)** |
| 嘟嘟利 | 樹果 | 73 | 72.1 → 94.7 **(+31%)** |
| 月亮伊布 | 技能 | 217 | 61.9 → 68.0 (+10%) |

**影響 45 隻**：活力填充S 34 隻 ＋ 月光（月亮伊布）1 隻 ＋ 10 隻萬用技能（`WILDCARD`：Metronome／Skill Copy／Versatile —— 隨機技能的平均值裡本來就含一份自回活力，變動 0.1~0.7%）。其餘 **201 隻逐位不變**，改動前後直接比對過。

四件事：

1. **自回活力要走 `simulate` 的第五個參數 `selfEnergy`**，只加進**那一隻**的 `supportPerStep`。
2. **它需要定點迭代**：活力↑ → 幫忙間隔↓ → 幫忙次數↑ → 技能發動↑ → 活力↑。單調遞增而且有上限（活力 150、`energyF` 最快 0.45），所以會收斂，實測 2~3 圈。**只有帶 `energySelf` 的那 45 隻要多跑**，其餘成本完全不變。因為 `pay` 要先算完才知道有沒有 `energySelf`，`memberOutput` 裡 `simulate` 的呼叫**搬到 `pay` 之後**（那些 `pay` 調整全部只看 `ctx`，不看 `sim`，所以是等價的）。
3. **`energyGiven` 從此只算發給隊友的那一份**（`energyTeam*5`），自回的另外走 `energySelfGiven`。`pickReason` 兩者分開顯示 —— 併成一句就是把「補師」和「自給自足」混為一談。
4. **負電（`Minus (...)`）刻意維持原行為。** 快照裡它也是 `energy` 欄位，但欄位語意（正電給食材、負電**給隊友**能量）說它是發出去的，而遊戲內說明沒有再確認過對象。所以 `memberOutput` 把它轉成 `energyTeam = energySelf/5`，數值與改動前**逐位相同**（第 11m 節與正負電同隊的比對都驗過）。轉換的目的只是讓 `energySelf` 這個名字從此只代表「真的只回自己」。

**哪些技能是「發給隊友」而不是自回**（`skillPayload` 已經處理，不要重複改）：`Energy For Everyone`（全體）與 `Energizing Cheer` 系列（隨機 N 隻，`energyTeam = energy × 目標數 / 5`）都會被轉成 `energyTeam`。留下 `energySelf` 的只有活力填充S、月光，以及萬用技能平均進去的那一份。

### 6e. 收取區間：這個遊戲不會自動收取（2026-09-09）

**遊戲機制（使用者確認）：不是自動收取，要上線點才收；主技能最多累積 2 次**（技能專長，其他 1 次）。所以產出是按「兩次收取之間」結算的，每一段有兩個上限：

| | 上限 | 超過之後 |
|---|---|---|
| 樹果／食材 | `helpsTillFull = carry / dropPerHelp` | 「偷吃」：只產樹果、食材歸零 |
| 主技能 | `bankedProcs`（技能專長 2、其他 1） | 拿不到 |

以前**只有夜間**套這兩個上限，白天完全不套 —— 等於假設整個白天隨時在收取。`simulate` 現在把白天也切成 `wakeH / wk.collectH` 段，和夜間走同一個 `segment()`。

**這個偏差只打在「技能觸發率高」的身上：**

| | 觸發率 | 隨時收 | 每 3h | 每 4h | 每 6h |
|---|---|---|---|---|---|
| 哥達鴨 | 20.4%（全 dex 第 2） | 18.53 | 12.33 | **9.75（53%）** | 7.17 |
| 風速狗 | 7.6% | 10.69 | 10.69 | 9.05 | 6.65 |
| 達克萊伊 | 3.8% | 4.08 | 4.08 | 3.45 | 2.53 |

⚠ **上面那張表是在「有隊友加成」的情境下量的，不要拿它去寫單隻的測試**（2026-09-10 更正）。
單獨一隻的哥達鴨（Lv60、溫順、技能觸發率M、無緞帶、無露營券）白天只幫忙 **32.3 次**、
`effSkill` **0.204**，所以：

| `collectH` | 0 | 1 | 2 | 3 | 4 | 6 | 8 |
|---|---|---|---|---|---|---|---|
| `procs`／日 | 8.59 | 8.59 | 8.59 | 8.59 | 8.59 | 7.17 | 5.88 |

**4 小時以下完全碰不到 `bankedProcs = 2` 的天花板**（每段只有 1.7 次發動），所以那幾格
逐位相同 —— 不是壞掉。第 2d 節以前照 18.53 那組數字寫，用 3h 去比，等於**什麼都沒測到**
（而且它同時還有 `sp` 傳名字不傳索引的 bug，根本跑不起來）。現在改成 6h／10h。
**寫這類測試之前先把 `helpsDay × effSkill ÷ 段數` 算一遍，確認真的會撞到 cap。**

**滿包之後不是白幫忙 —— 這兩行不要「順手」改**（2026-09-10 查證，使用者問「食材掉落發動確定不會有能量嗎」）

來源：日文驗證 wiki [おてつだい](https://wikiwiki.jp/poke_sleep/%E3%81%8A%E3%81%A6%E3%81%A4%E3%81%A0%E3%81%84)。所持數到上限後進入「いつのまに育成」：

> 「この状態になったポケモンのおてつだいでは食材を拾ってくる確率が **0％** となり、きのみを拾ってくる確率が **100%** になる」
> 「最大所持数を超えた分のきのみを**自動的にカビゴンに与えてエナジーに変換する**システム」
> 「…また**メインスキルの発動判定も行われなくなる**」

| 滿包之後 | 遊戲 | 引擎 |
|---|---|---|
| 食材 | 機率 0% | `berries` 沒把 `snack` 算進食材，`ing` 只吃 `productive` ✓ |
| 樹果 | 機率 **100%**，自動餵給卡比獸換能量 | `snack * berriesPerDrop`，**刻意不乘 `(1-ingChance)`** ✓ |
| 主技能 | **發動判定不做** | `procs` 用 `normal` 而不是 `h` ✓ |

兩個都很容易被當成 bug「修掉」：`snack*berriesPerDrop` 看起來像漏乘了 `(1-ingChance)`，`Math.min(normal*effSkill, ...)` 看起來像該用 `h`。**兩個都是對的。** 這也是 `fillH` 與 `skillH` 要分開顯示的理由 —— 背包滿只漏食材，技能存滿漏的是另一件事。

觸發率越高越容易撞到「每段 2 次」的天花板，所以高頻技能型被系統性高估、推進推薦名單 —— 那正是使用者一路質疑「技能型是不是太理想」的來源。同一支隊伍的總能量：3h **−6.9%**、4h **−15.4%**、6h **−27.1%**。

**反直覺的一點：收得越不勤，食材型也吃虧。** 袋獸的持有上限只有 21（全 dex 最低那一批），6 小時就會裝滿掉 26%，於是料理跟著掉、技能隊反而反超。實測 `collectH=6` 時最佳陣容從「樹果3/食材1/技能1」變成「樹果1/技能4」。

三件事：

1. **`wk.collectH` 沒有值時走「隨時收」的舊行為**（`collectH = 0` 分支）—— 舊的雲端資料不會突然變一個數字出來。
2. **`SCORE_WK` 固定 `DEFAULT_COLLECT_H`**，不跟著使用者的週設定跑（個體產能要跨週可比），但**一定要有值** —— 否則寶可夢箱裡技能率高的那幾隻會像推演以前那樣被高估。
3. `simulate` 回傳多了 `dayProcs` / `nightProcs` / `daySnack` / `nightSnack`，UI 要拆開顯示時不必重算。
4. **`fillH` / `skillH`：把「該多久上去收一次」直接算出來**（2026-09-10，使用者要求）。
   `snack` 是**事後結果**（你已經漏了多少），使用者要的是**該怎麼做**。見「多久該上去收一次」那一節。

### 6d. `productive` 含夜間，但**技能發動不含**（2026-09-09）

`simulate` 裡有兩個不同的「幫忙次數」，用錯會靜靜地偏袒某一種專長：

| 用途 | 該用哪個 | 為什麼 |
|---|---|---|
| 樹果 `berries`、食材 `ing` | `productive`（= `helpsDay + nightNormal`） | 睡覺時撿的東西**醒來會收到** |
| 主技能發動 `procs` | `helpsDay` ＋ `min(bankedProcs, nightNormal*effSkill)` | 睡眠期間只累積、醒來最多結算 1~2 次 |

**踩過：** `procs` 原本寫成 `productive*effSkill + min(banked, ...)` —— 夜間幫忙先被完整
乘過一次 `effSkill`（無上限）、再加一次 banked，同一批算了兩次。`bankedProcs` 這個上限
的存在本身就承認夜間要被 cap，所以那兩項是互相矛盾的。

後果是**偏差只打在技能型身上**：技能能量高估 **20.8%**（AMPHAROS 週能量 190,026 →
150,468），而樹果型的樹果收入與食材型的食材顆數**完全不變**。實測會把「樹果4/食材1」
擠下去換成技能型，食材型幾乎不入選。量測與捨棄的替代模型見 `DECISIONS.md`。

**加新的「每次幫忙都有機率發生」的效果時，先問它在睡眠期間到底會不會發生。**

### 6. 記憶化的 context key

`ctxKey()` 決定 `memberOutput` 的快取粒度。加新的 team-level 效果時**一定要加進 `ctxKey`**（`hbRows` 是 map，要用 `hbKey()` 排序後序列化，否則同一組合會因為鍵的順序不同而各存一份），否則會拿到別的隊伍組成算出來的結果。`supportEnergy` / `extraHelps` 有量化（`qE` / `qH`）來控制快取爆炸。

#### ⚠ 但「加進 `ctxKey`」有一個前提：那一項必須**有界**（2026-09-10，使用者的分頁 OOM 崩掉）

`ctxKey` 裡的每一項不是量化過（`qE` 15 的倍數且上限 120、`qH` 0.5 的倍數、
`darkDrain` 3 的倍數）就是本來只有幾種值（`nHB` 0~5、`nDragon` 1~5、四個布林）。
**那不是巧合，是這個快取能成立的條件。**

放一個**連續值**進去（`mateBerryPow` ＝ 整隊五隻的樹果能量總和，幾乎每一隊都不同），
memo 就從「收斂在幾千筆」變成**跟組合數線性成長**，每組合約 0.5~0.9 筆：

| 箱子 | 組合數 | memo（有 `mateBerryPow`） | memo（沒有） | 倍數 | 單執行緒耗時 |
|---|---|---|---|---|---|
| 24 隻 | 42,504 | 11,200 | 48 | **233×** | — |
| 30 隻 | 142,506 | 82,362 | 1,103 | 75× | — |
| 36 隻 | 376,992 | 348,552 | 5,479 | 64× | — |
| 42 隻 | 850,668 | 487,899 | 6,415 | 76× | **62.1s → 17.8s** |

使用者 89 隻的箱子 ＝ **C(89,5) = 41,507,642 組**，跑到 **25%** 時分頁就
`Out of Memory` 崩掉。**dedicated worker 和主執行緒共用同一個 renderer 行程**，
所以六個 worker 的快取是**加總**的 —— 這也是「加速只有 2x」以外，worker 池的
第二個隱藏成本。

**修法不是量化**（那會讓數字失真），而是**把那一項移出記憶化**：

- `memberOutput` 回傳 `mateBerryCoef`（＝ `procs × teamBerry`）與 `ownBerryPow`，
  **不**把隊友那一份加進自己的 `berryStrength`
- `scoreTeam` 事後補：`mateBerryAdd(o, ctx)` ＝ `係數 × (隊伍總和 − 自己那一份)`
- 這是**恆等變換**，不是近似 —— 那一項對每個成員都是「只跟牠自己有關的係數」乘上
  「一個團隊純量」，所以拆得開。`npm run verify` 的第 1 名總分逐位不變。

**判準：這個效果對成員的貢獻，能不能寫成「成員自己的係數 × 團隊純量」？**
可以就移出去（快取鍵乾淨、數值精確）；不行才進 `ctxKey`，而且**進去之前必須先量化**。

代價與配套：

1. **memo 現在被 `mateBerryPow` 不同的隊伍共用**，所以漏補任何一處，答案就會取決於
   「這個 memo 之前算過誰」—— 靜靜地算錯。第 5d 節直接比「共用 memo」與
   「每隊一份新 memo」，任何一組對不上就紅。
2. **memo 裡的物件是跨隊共用的，不可以就地改。** `scoreTeam` 用淺拷貝
   （`{...o, berryStrength: …}`），而且**只有係數非 0 的那一隻要拷貝** ——
   一支隊伍最多一個。
3. **UI 只能讀 `r.outs[k]`**（scoreTeam 產出的調整後副本），不可以直接拿
   `memberOutput` 的回傳值去顯示樹果產出。目前 `app.js` 全部走 `r.outs[k]`。
4. 第 5d 節同時有**反向守門**：`ctxKey` 餵不同的 `mateBerryPow` 必須得到**相同**的鍵。

### 7. 副技能的欄位解鎖

`SS_SLOT_LV = [10,25,50,70,80]`。`activeSubskills()` 只採計 `m.level >= SS_SLOT_LV[i]` 的欄位 —— **順序有意義**，`m.ss` 的陣列位置就是遊戲裡的欄位位置。

### 8. 文案不能寫死檔名

拆檔時踩過：UI 還在教使用者「替換 `index.html` 裡的 `<script id="gamedata">`」，但那個區塊早就不存在了。README 和這份檔案也各有一處。**讀程式碼看不出來，只有截圖才會發現。**

所以使用者可見文案裡的檔名與指令，一律從 `app.js` 的 `PATHS` 取（`P.data`、`C.rebuild` …），不要寫死字串。`tests/smoke.mjs` 的「文案一致性」那一節會斷言 `PATHS.files` 的每個路徑真的存在、`PATHS.cmds` 的每個指令真的定義在 `package.json`，並反向掃文案裡有沒有 `PATHS` 以外的硬寫路徑。

### 9. 資料與程式的版本偏移

`app.js` 的 `SCHEMA` 必須等於 `data/game.json` 的 `meta.schema`。**動到資料的欄位結構時兩邊一起 +1**（純數值更新不用動），`tools/extract-data.mjs` 裡也有一份要同步。

理由：拆檔後 `app.js` 與 `game.json` 是兩個獨立快取的資源，GitHub Pages 送 `max-age=600`，所以更新後有最多 10 分鐘的窗口會拿到「新程式 ＋ 舊資料」。不擋的話使用者只會看到壞頁面或錯的數字，不知道重新整理就好。

**`app.css` 與 `src/*.js` 之間也有同樣的偏移，但它們沒有 schema 可以斷言。** 實際踩過一次：改完箱子版面之後，瀏覽器拿到新的 `index.html`（篩選列出現了），但 `app.js` 和 `app.css` 都還是舊快取 —— 畫面看起來像「你根本沒改」。而且從截圖才看得出來：種類選單顯示 `大食花　#71 Vi…`（新版沒有英文名）、副技能顯示 `技能…`（新版用全名）。

所以資源都帶 `?v=` 版本號，而且**四處必須一致**：

| 位置 | 用途 |
|---|---|
| `index.html` `<head>` 的 `app.css?v=…` | CSS 的快取破除 |
| `index.html` 載入器裡的 `ASSET_V` | 加到三個 `src/*.js` 的 query，並放到 `window.ASSET_V` |
| `src/app.js` 的 `APP_V` | app.js 自己的版本；和 `window.ASSET_V` 不符就 `fatal()` |
| `src/engine.js` 的 `ENGINE_V` | 引擎自己的版本；主執行緒與 **Worker 兩側**都比對 |

第三、四個是關鍵：光靠 `?v=` 只是**降低**拿到舊檔的機率，斷言才能保證「過期的檔案不會靜靜地畫出舊版 UI／算出舊版數字」。第二次踩到就是因為只有前兩個 —— CSS 更新了、JS 沒更新，畫面看起來像沒改。

#### Worker 是唯一一條 `ASSET_V` 擋不到的路徑（2026-09-10 修）

`new Worker(url)` 與 worker 裡的 `importScripts()` 走的是**另一條快取**，主執行緒
`<script src="./src/engine.js?v=…">` 完全管不到它。以前兩者都沒帶 `?v=`，所以部署後那個
窗口裡會出現：

> worker 用**舊 engine.js** 列舉評分 → 主執行緒用**新 engine.js** `rehydrate()` 與跑決賽

兩套公式，**不會報錯**，只是分數對不起來、決賽名單是照舊公式選的。這正是 `APP_V`
當初要擋的那一類，而 worker 路徑整個漏掉了。三件事都要做：

1. `new Worker('./src/engine.worker.js' + VQ)` —— `VQ` 由 `window.ASSET_V` 組出來
2. `init` 訊息帶 `v`，worker 用它組 `importScripts('./engine.js?v=' + v)`
3. **真的去比對**：worker 的 `ready` 訊息回報 `ENGINE_V`，對不上就 `killPool()` ＋ `fatal()`

`tests/smoke.mjs` 第 10c 節斷言這三件事，而且斷言 `performance.getEntriesByType('resource')`
裡實際載入的那兩個檔真的帶著對的 `?v=`（不是只有原始碼寫對）。

**動到 `app.css` 或 `src/*.js` 就把版本號往前推（四個地方一起改）。** 純資料更新不用動。`tests/smoke.mjs` 第 11 節會斷言前三處一致並且跑起來的那一份真的相符，第 10c 節管 worker 那一側。

### 10. 遊戲顯示的主技能等級是「加成後」，`m.skillLv` 是「基礎值」

`baseStats` 算的是 `skillLv = clamp(m.skillLv + (Skill Level Up M?2:0) + (Skill Level Up S?1:0))`，所以 `m.skillLv` 存的是**基礎值**。但遊戲的技能卡顯示的是**加成後**的等級 —— 大食花畫面上是 `Lv.6` 且帶「技能等級提升M」，基礎值其實是 **4**。直接把畫面數字存進 `m.skillLv` 會讓技能產出高估兩級。

而且**不能靠「假設遊戲顯示的是加成後」來反推** —— 那是猜的。真憑據是技能說明裡的數字：

| 畫面 | 資料 | 結論 |
|---|---|---|
| 活力填充S Lv.6「回復活力43」 | `ms['Charge Energy S'].energy[5] = 43.4` | 有效等級 = 6 |
| 食材獲取S Lv.3「隨機獲得11個食材」 | `ms['Ingredient Magnet S'].ingredient[2] = 11` | 有效等級 = 3 |

所以 `impEffFromPayload` 用說明數字**反查**有效等級，再減掉副技能加成得到基礎值。有效等級已達上限時基礎值只能推到下界，但那時候再高也一樣（`clamp` 會壓回上限），不影響計算 —— UI 會把這件事寫出來。

妙蛙花是第二個獨立憑據，而且方向相反：畫面 `食材獲取S Lv.5`、說明「隨機獲得17個食材」（`ingredient[4] = 17` → 有效 5）、而「技能等級提升M」在第 3 格（Lv50 解鎖）且牠 Lv61 **加成生效**。畫面顯示的 5 正好等於有效等級 → 基礎值 3。

### 範圍型技能：區間就是 `[v/2, 2v]`

遊戲對某些能量填充類技能顯示的是一個**區間**，而快照只存一個固定值：

| 畫面 | 快照 | 關係 |
|---|---|---|
| 能量填充S Lv.3「卡比獸的能量增加393〜1,570」 | `ms['Charge Strength S'].strength[2] = 785` | `393 = round(785/2)`、`1570 = 785×2` |
| 能量填充S Lv.2「卡比獸的能量增加285〜1,138」 | `strength[1] = 569` | `285 = round(569/2)`、`1138 = 569×2` |

所以 `impEffFromPayload` 是**兩段式**：第一段拿 payload 精確比對；只有第一段一個都對不到時才啟用第二段（拿 `v/2` 與 `2v` 比）。這樣原本就有唯一解的情形完全不受影響，而區間的兩端都反解得出來。

**這個放寬一定要有全域掃描擋著。** 錯的技能等級**不會讓任何校驗碼破掉**（幫忙間隔與持有上限都不吃 `skillLv`），所以沒有測試就沒人擋得住。第 11b 節會掃過 `D.ms` 全部技能 × 全部等級的兩端並斷言「唯一但錯 = 0 筆」（目前是唯一且正確 298、多解退回 126、精確比對就有解 146）。

**改動 `import.js` 的主技能等級推導時，七隻 golden case 都要重跑。**

### 陷阱：還沒解鎖的食材格也一定要解

`baseStats` 只讀 `slots = min(floor(level/30)+1, 3)` 格。所以 `impIngSets` 原本會跳過超出的格子，讓它留在預設的「選項 0」。

**實際踩過：** 耿鬼 Lv50 的第 3 格畫面預告的是「品鮮蘑菇×6」，跳過的話會存成「火辣香草×7」。而因為那格還沒生效，**兩個校驗碼、樹果顆數、任何產出數字都不會變** —— 一路等到升上 Lv.60 才會發現，而那時候已經沒有截圖了。

遊戲畫面本來就把它預告出來（🔒Lv.60 加食材圖與 ×N），所以 `ingCounts[2]` 本來就有那個數字：三格一律用 ×N 收斂，回傳的 `slots` 只給 UI 標「這格還不生效」。同理，箱子卡片的未解鎖食材格是**變淡但照樣顯示、照樣可改**（和副技能同一個處理方式）—— 以前是 `disabled` ＋ 顯示「未解鎖」，結果截圖校對時那一格根本改不動。

## 資料重建

遊戲資料是靜態快照（見頁面「資料版本」的 commit 與日期）。重建：

```bash
node tools/extract-data.mjs        # 會印出用法
```

流程：clone 上游 → 用 esbuild bundle 萃取腳本 → 合併 `tools/zh.txt` → **直接覆寫 `data/game.json`**（indent-2）。

覆寫之後一定要 `git diff data/game.json` 看一眼再 commit —— 那份 diff 現在是一個欄位一行，上游動了什麼會直接顯示出來。

**`zh` 區塊上游沒有** —— 它來自 `tools/zh.txt`（遊戲自己的 i18n 字串）。重建時絕對不能弄丟。

### ⚠ 這支腳本 2026-09-10 之前**跑不起來**，修過兩個 bug

第一次真的執行它（上一次成功重建是 2026-09-07，之後 `84b8035` 加了 msExtra 那一段）就炸了兩次。
兩個都屬於「不執行就看不出來」的那一類：

| bug | 症狀 | 為什麼躲過所有審查 |
|---|---|---|
| `if (!ms[k])` | `ReferenceError: ms is not defined`，整支腳本死在寫檔之前 | `ms` 是**萃取用的 `extract.ts` 裡**的變數，`.mjs` 這一層只有 `data.ms`。兩個檔在同一支腳本裡，名字看起來很合理 |
| `zhTxt.split('
')` | 465 個中文名全部帶一個**尾隨 ``** | `core.autocrlf=true` 的 Windows checkout 會把 `tools/zh.txt` 變成 CRLF。旁邊解析 `types.txt` 的那一段本來就用 `/?
/`，只有 zh 這條漏了 |

第二個特別惡劣：**`` 在 HTML 裡不顯示**，所以畫面上「特選蘋果」和「特選蘋果``」長得一模一樣，
但它會讓任何字串比對、`zh` 反查、搜尋列的比對靜靜地失敗。

推論兩件事：

1. **`data/game.json` 的 `msExtra` 是手動補進去的**，不是這支腳本產出的（腳本在那之前就 throw 了）。
   內容經比對正確，但那表示「腳本能跑」從來沒有被驗證過。
2. **`data.ms[k]` 那道守門現在才真的生效。** 它的用途是「`skills-extra.json` 提到的主技能
   在上游消失或改名時要當場炸掉」—— 而上游**這一次就真的改名了**（見下）。

### 重建之後要驗什麼

`git diff data/game.json` 只看得出「哪幾行變了」，看不出「數字會不會變」。2026-09-10 那次上游更新
（`7814857` → `70724a2`）就是個好例子 —— diff 有 869/872 行，但實際只有三件事：

| 改動 | 影響 |
|---|---|
| `recipes` **陣列順序整個重排** | 內容與集合完全相同。持久化全部用**食譜名稱**當鍵（`wk.recipeName`、`wk.recipeLevels[r.n]`），所以既有資料安全；但貪婪排程的**同分 tie-break 吃陣列順序**，所以要實測 |
| 5 隻的主技能 `Dream Shard Magnet S Range` → `Dream Shard Magnet S`（沙漠蜻蜓／食夢夢／夢夢蝕／勾魂眼／溶食獸） | 舊條目是 `{max:8}`（沒有任何數值陣列），新條目有 `shard[]`。引擎根本不讀 `shard`（夢之碎片不計分），所以純粹是名字 |
| `meta` 的 commit／日期 | — |

**驗法：拿新舊兩份 `game.json` 各跑一次引擎，逐位比對。** 兩個層級都要：

- **全 dex 掃 `monPower`**（246 隻 × 2 個等級）→ 抓得到「某一隻的數值悄悄變了」
- **`searchTeams` 前 5 名的 `score` / `dishS` / 成員**，跨幾個 `collectH` → 抓得到排序與 tie-break

⚠ **測試用的食譜等級一定要由「食譜名稱」決定，不能用陣列索引。** 第一次量的時候寫的是
`recipeLevels[r.n] = 10 + (i % 30)`，而上游剛好把順序重排了 —— 於是兩邊每一道菜的等級都不一樣，
量出來「分數全變了」。那是測試自己的 artifact，不是資料差異。改用名稱的雜湊之後**逐位相同**。

## 不要做的事

- **不要把真實的 Apps Script token commit 進 `Code.gs`** —— 永遠保持 `CHANGE_ME_TO_A_LONG_RANDOM_STRING`。真實金鑰只在 Google 的編輯器裡改
- **不要移除 `LICENSE` / `NOTICE`** —— 遊戲資料是 Apache-2.0，要求保留歸屬
- **不要在 `zh` 對照表裡自創中文名** —— 沒有來源就留英文，並在 `已知簡化` 註明
- **不要移除 `已知簡化` 那一段** —— 那是這個工具可信度的基礎；改了行為就同步改它

## 驗收方式

```bash
npm i playwright-core
npm test                         # smoke：引擎、單調性、窮舉不變量、雙後端、Sheet 往返（含離線沖出、失敗重試、「資料存在哪裡」的文案）、Worker（含版本斷言）、截圖匯入、箱子篩選／排序（含雙向）／練滿・資質・技能成長三個投資數字／重複偵測／暱稱與 escape、食譜解鎖（沒解鎖的不進池子／選單／空狀態／撞牆時的訊息）、JSON 追加／取代、自組隊伍（與推演逐欄位一致／索引維護／選擇器／箱子的「＋隊」與按鈕圖示）、主技能「回自己活力」不攤給全隊、發給隊友的樹果用隊友自己的、結果卡三句話的取捨與代價不被擠掉、收取間隔（滿包／技能存滿／整隊建議）、推演結果過期要作廢並講出來、文案與資源版本一致、390px 不橫向捲動、schema 偏移
npm run verify                   # 慢速（數分鐘）：大箱子的順序不變性、FINALISTS 夠不夠
```

`run()` 是**非同步**的（推演跑在 Worker 裡）。測試等結果時**不要用 `waitForTimeout`** —— 在慢一點的機器上會 flaky。用 `smoke.mjs` 裡的 `doRun()` 輔助函式，它會先清掉 `lastResults` 再等它被填回來。

### ⚠ 寫測試時最容易踩的五個坑（2026-09-10，一次修掉六節從來沒跑過的測試）

`recipeOn` 那次改動之後整套測試從第 2 節就 timeout，補上去才發現底下這些**從一開始
就不可能通過** —— 也就是說那幾節斷言的東西一直沒有人在守。

1. **`doRun()` 一定要先解鎖食譜。** 「沒填等級 ＝ 沒解鎖 ＝ 不進池子」是產品規則，
   而 `wk.recipeLevels` 預設是空的 → `run()` 直接拒跑 → `waitForFunction` timeout。
   `doRun` 現在會把**沒填過的**補成 20（第 3 節的單調性測試才還能自己指定等級）。
   不走 `doRun` 而自己組 `wk` 的地方（第 4 節、`verify.mjs`）要自己解鎖。
2. **`m.sp` 是 dex 索引，不是物種名。** 直接寫 `{sp:'GOLDUCK'}` 的話
   `D.dex['GOLDUCK']` 是 `undefined`，`baseStats` 讀 `.cs` 就 throw。
   走 `deserialize({roster:[...]})` 的地方沒事（`reviveSp` 會轉），**自己組 member
   物件的地方一定要 `D.dex.findIndex(x => x.n === n)`**。
3. **`page.evaluate(fn, a, b)` 只吃一個參數**，多傳直接 throw。要多個就包成物件。
4. **不要回傳「之後才呼叫」的 closure。** 第 5b 節踩過：`go()` 覆寫全域 `roster`
   卻回傳 `out: i => memberOutput(roster[i], …)`，三組比較全讀到**最後一次**的
   roster，於是「負電配對 vs 單獨」兩邊都是 0，看起來像引擎壞了（引擎沒壞）。
   **當場把數字算出來再回傳。**
5. **`deserialize()` 不會重畫。** 之後直接 `showView('box')` 的話，
   `idealFillAsync → applyBoxFilter` 會讀到上一節留下的 `data-i`。**先 `renderBox()`。**

還有一個不是 bug 但同樣致命的：**斷言要選一個真的會動的量**。第 6b 節比
`wakeEnergy`，但沒有補師又睡 8.5 小時的隊伍五隻**每天都會歸零**（見 6g，那是對的），
所以那條斷言恆為 false；改比 `fastShare` 才量得到扣活力。同理第 2d 節的收取間隔要選
**真的會撞到 `bankedProcs` 上限**的間隔（見陷阱 6e 那張更正表）。

`tests/smoke.mjs` 會**自己起一台靜態 server**（因為資料改用 fetch 之後不能再用 `file://` 載入），所以 CI 不需要額外的 server step。

Chromium 由 `tests/chromium.mjs` **自動尋找** —— playwright-core 下載的那份、系統裝的 Chrome/Edge、容器路徑都會試。要指定才設 `CHROMIUM` 環境變數。

（別把 `CHROMIUM=...` 前綴當成常規用法：那會讓 `.claude/settings.json` 的 `Bash(npm test)` 允許規則匹配不到，每次都得手動確認。）

CI 在 `.github/workflows/ci.yml`，push / PR 都會跑。

**測試對公式係數的覆蓋是「一半」，要知道缺哪一半。**

截圖匯入那一節（第 11b）是目前唯一有**絕對數值**的斷言 —— **七隻**真實寶可夢的幫忙間隔與持有上限是逐欄手算對照過的期望值，不是程式算完存回去的：

| 隻 | 等級／性格 | 幫忙間隔 | 持有上限 | 緞帶 | 這隻壓的邊界 |
|---|---|---|---|---|---|
| 大食花 | 60 頑皮 | 1911s | 35 | 4 | 進化階段（喇叭芽／口呆花的基礎頻率不同） |
| 水箭龜 | 62 馬虎 | 2458s | 65 | 4 | `Inventory Up M`＋`L`；食材第 3 格歧義 |
| 嘎啦嘎啦 | 55 害羞 | 2943s | 26 | 3 | **無修正性格**（`nat.f = 1`）；食材第 2 格歧義 |
| 耿鬼 | 50 怕寂寞 | 1410s | 40 | 3 | **範圍型主技能**；**未解鎖的第 3 格** |
| 嘟嘟利 | 53 害羞 | 1772s | 50 | 3 | 主技能 Lv.1（下界）；`Inventory Up L` |
| 隆隆岩 | 60 怕寂寞 | 1944s | 27 | **1** | `RIBBON_CARRY` 的 `+1`（其他六隻都不是 1） |
| 妙蛙花 | 61 樂天 | 2464s | 35 | 4 | 技能等級加成**生效**時的基礎值推導 |

還有**第三個獨立校驗碼**：畫面上的「樹果 ×N」（樹果專長 2 顆 ＋ `Berry Finding S` 1 顆）。它不吃幫忙間隔也不吃持有上限，所以壓的是 `berriesPerDrop` 那條路徑。

所以動到這些會紅：

- `helpInterval` 的 `1 - 0.002*(level-1)`、`2 - nat.f`、幫忙速度副技能的 `0.14 / 0.07`、`ribbonFreqMul`
- `baseStats` 的 `carry` 算式、`RIBBON_CARRY`、`Inventory Up S/M/L` 的 `6/12/18`
- `berriesPerDrop` 的 `(berry||all ? 2 : 1) + (Berry Finding S ? 1 : 0)`
- 性格與副技能的數值表

**但能量／模擬／料理那一側還是沒有絕對基準。** 把 `energyF` 的 `0.45` 改成 `0.50` 依然全過 —— 那條路徑不影響幫忙間隔也不影響持有上限。要擋住那一半還是得做**引擎輸出的快照測試（golden file）**，見 `TODO.md` 第 2 項。

動引擎時的臨時替代做法：改之前先跑一次固定 seed、記下 `lastResults[0].total`，改完再比。Worker 重構就是這樣驗證數值等價的（`804479.78`，前後完全一致）。
