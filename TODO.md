# TODO

按優先順序。每項附**動機**、**做法**、**驗收**。

---

## 0. 用真實的箱子跑一次

**優先度：最高。這不是程式工作，但它決定其他每一項該不該做。**

**動機**：`DECISIONS.md` 誠實記著一件事 —— **這個工具從來沒跑過真實資料**。所有測試都用捏造的寶可夢，交接時箱子是空的。

引擎的產品承諾是「隊伍之間的排序可靠」，但那個承諾**從未被人眼驗證過**。如果把真實的箱子輸進去，推出來的前 8 名有明顯不合理的（例如某隻你確定很強的從沒上榜），那就是引擎有系統性偏誤 —— 而那會讓下面所有項目的優先序重排。

**做法**：把實際持有的寶可夢建進箱子（等級、性格、五格副技能、三格食材、緞帶），用本週真實條件推演，然後**逐一看前 8 名合不合理**。特別注意：
- 有沒有哪隻你直覺很強但完全不上榜
- 補師（活力類）的評價是否明顯偏高 —— `DECISIONS.md` 已知補能量的平均攤法「對補師略微樂觀」
- 食材型寶可夢的排名是否被「食材精選池未實作」那一項影響

**驗收**：能對前 8 名的每一組講出「為什麼合理」或「這裡不對」。不對的地方開成新的 TODO 項目。

---

## ~~1. 拆檔：`data.json` + `app.js` + 精簡 `index.html`~~ ✅ 已完成

原本 156 KB 的單檔拆成：

| 檔案 | 大小 |
|---|---|
| `index.html`（骨架 ＋ 載入器） | 11 KB / 250 行 |
| `src/app.js` | 50 KB / 1,025 行（後來第 4 項又拆出 `src/engine.js`） |
| `src/app.css` | 10 KB / 147 行 |
| `data/game.json`（indent-2） | 193 KB / 14,032 行 |

資料改用 `fetch('./data/game.json')`，`await` 完才動態插入 `app.js`。**`app.js` 刻意保持 classic script**（不是 module）—— 頂層宣告必須留在全域，否則 `tests/smoke.mjs` 全滅。細節見 `CLAUDE.md` 的「載入順序」。

**驗收結果**：
- ✅ 27 項 smoke 全過（測試改成自己起靜態 server）
- ✅ 改一行 `energyF`，`git diff` 只顯示那一行
- ✅ 改 `BULBASAUR` 的 `f`，diff 顯示 `"f": 4400 → 4300`，上下文看得到是哪隻
- ✅ `file://` 直接開會顯示「請跑 `npm run serve`」而不是白畫面

代價：`file://` 不能用了，本機要 `npm run serve`。GitHub Pages 不受影響。

---

## 2. 引擎輸出的快照測試（golden file）

**優先度：高。這是唯一能擋住數值迴歸的東西，而它現在不存在。**

**動機**：CI 骨架已完成（`.github/workflows/ci.yml`，push / PR 跑 46 項），但**原本寫的驗收條件沒過**。

實測把 `energyF` 的 `0.45` 改成 `0.50` —— **46 項依然全過，CI 不會紅。** 現有斷言全是結構性（「21 餐加總正確」）或相對性（「同樹果隊拿到更多幫手」），基準值在執行時才算，所以動係數完全抓不到。

這很嚴重：`DECISIONS.md` 列的 8 個已修 bug 裡有 4 個是數值錯誤（幫手加速低估 49%、揮指算 0、料理能量低估 27%、負電無條件給能量）。**同一類 bug 再發生一次，現有測試不會響。**

**做法**：固定一組隊伍與週條件（沿用 `smoke.mjs` 的 seed），把引擎輸出存成 `tests/golden.json`：
- 隊伍層級：`total` / `berryS` / `dishS` / `potEff`
- 每位成員：`fastShare` / `berryStrength` / `skillStrength` / `ingredient` 向量

比對到小數第 2 位。失敗時**印出變化幅度**（`total: 804480 → 812310 (+0.97%)`），而不只是「不相等」—— 這樣看訊息就知道影響多大。確認是刻意改動就跑 `npm test -- --update` 更新 golden file。

**驗收**：把 `energyF` 的 `0.45` 改成 `0.50`，`npm test` 必須失敗，且訊息印出變化幅度。

---

## ~~3. 快取偏移：`app.js` 與 `game.json` 可能版本不一致~~ ✅ 已完成

拆檔引入的問題，單檔時代不存在。

**做法**：`data/game.json` 的 `meta` 加了 `schema: 1`，`app.js` 有一份 `SCHEMA` 常數斷言相等，不合就整頁換成「請強制重新整理（Ctrl+Shift+R）」。載入器另外改用 `fetch(..., {cache:'no-cache'})` 強制向伺服器驗證（通常回 304），並檢查必要的頂層鍵都在。`tools/extract-data.mjs` 也會寫出 `schema`。

**驗收結果**：
- ✅ 測試把 `meta.schema` 改成 999 送給瀏覽器，頁面顯示版本不符與強制重新整理的說明
- ✅ 沒有硬撐著算出數字（`.wrap` 整塊被換掉）
- ✅ schema 正確時照常啟動

**動資料結構時記得兩邊一起 +1**（`app.js` 的 `SCHEMA`、`extract-data.mjs` 的 `schema`）。純數值更新不用動。

---

## ~~4. 推演搬進 Web Worker~~ ✅ 已完成

**做法**：引擎抽成 `src/engine.js`（純函式、不碰 DOM），主執行緒與 worker **載入同一份**，所以不會有兩份引擎走鐘。`src/engine.worker.js` 只是薄殼，`importScripts('./engine.js')` 之後呼叫 `searchTeams()`。

一併處理掉的事：
- `rlvl` / `buildPool` / `rankRecipesForTeam` 原本直接讀全域 `wk` —— 改成參數（worker 裡沒有那個全域）
- `wk.recipe` 的物件參照問題：`searchTeams` 自己從 `wk.recipeName` 解析，根本不用送過去
- **取消改用 `terminate()`**。搜尋是同步迴圈，跑的時候 worker 不處理訊息佇列，送 `cancel` 進去要等搜尋結束才被讀到；`SharedArrayBuffer` + `Atomics` 需要 COOP/COEP 標頭，GitHub Pages 給不了
- `terminate()` 之後那個 pending promise 永遠不會 settle → `killWorker()` 主動 reject 掉
- 沒有 `Worker`（或 worker 載入失敗）會退回主執行緒同步跑，`comboCount` 標上「· 主執行緒」

**驗收結果**：
- ✅ 40 隻的箱子（C(40,5) = 658,008 組合）推演期間，點主題切換按鈕**立刻生效** —— UI 沒凍住
- ✅ 進度條會動（每 80ms 節流回報一次）
- ✅ 取消有效：結果被丟棄、狀態重置，而且**取消後還能再推演**（會重開一個 worker）
- ✅ 引擎輸出**數值等價**：重構前後第 1 名 `total = 804479.78`，逐欄位完全一致
- ✅ 46 項測試全過（測試也改成確定性等待，不再靠 `waitForTimeout` 撞運氣）

---

## ~~5. 文案一致性檢查~~ ✅ 已完成

**動機**：這個專案至今抓到的 bug 全是**跑起來**發現的，沒有一個是靠讀程式碼：

| Bug | 怎麼發現的 |
|---|---|
| `$` 的 TDZ → 整頁死掉 | 同步往返測試 |
| 貪婪排程不單調（調高等級反而變差） | 對照實驗 |
| 搜尋／評分目標脫鉤 → 料理能量低估 27% | 對照實驗 |
| 自架版本文案說謊（按鈕聲稱 Claude 會處理） | 看螢幕截圖 |
| 拆檔後文案還叫人「替換 `<script id="gamedata">`」 | 看螢幕截圖 |

**做法**：`app.js` 加了 `PATHS`（`files` ＋ `cmds`），使用者可見文案裡的檔名與指令一律從那裡取。測試第 10 節做三件事：斷言 `PATHS.files` 的每個路徑真的存在、`PATHS.cmds` 的每個指令真的定義在 `package.json`、**反向掃**文案裡有沒有 `PATHS` 以外的硬寫路徑。

**驗收結果**：
- ✅ 把 `src/engine.worker.js` 改名，測試失敗並指名 `worker=src/engine.worker.js`
- ✅ 把 `data/game.json` 改名，整套測試在載入階段就失敗

---

## 6. 多 worker 平行化 —— 順便拿掉預篩

**動機**：兩件事一起解決。

1. **速度**：worker 讓 UI 不凍住了，但**沒讓推演變快**。實測 40 隻 = 9.4 秒、60 隻全搜 = 25 秒（單執行緒）。箱子只會越來越大。
2. **正確性**：`PRESCAN_LIMIT` 那個預篩用的是有偏誤的啟發式（`scoreTeam([i] + 池子前 4 隻)`，companions 是按 roster 順序的任意 4 隻）。已經實測到 60 隻都沒丟解（見 `DECISIONS.md`），**但箱子到 100 隻是砍 100→42，風險是真的**。

**平行化之後可以直接把預篩拿掉** —— 用窮舉取代啟發式，這是「平行化買到的是正確性，不只是速度」。

**做法**：
- `combinations` 的列舉空間按第一個索引分片，開 `navigator.hardwareConcurrency` 個 worker
- 每個 worker 回傳自己的前 `FINALISTS` 名，主執行緒合併後再跑 `bestPlan` 決賽
- `memo`（`memberOutput` 的快取）**不能跨 worker 共用**，每個 worker 自己一份 —— 會重複算，但那是 CPU 換 wall-clock，值得
- 進度回報要匯總各 worker 的 `count`

**陷阱**：`searchTeams` 目前一個函式做完「預篩 → 列舉 → 決賽」三件事。分片需要把它拆成 `searchShard()`（可平行）和 `finalize()`（主執行緒合併），不然決賽會被跑 N 次。

**驗收**：
- 60 隻箱子全搜（546 萬組）在 8 核機器上 < 5 秒
- 拿掉預篩後，`trimmed` 永遠是 false，`comboCount` 顯示的組合數等於 C(n,5)
- 前 8 名與單執行緒全搜**逐欄位相同**（拿第 2 項的 golden file 比）

---

## 7. 食材精選（Ingredient Draw S）的專屬食材池

**動機**：真實技能從該物種的專屬清單抽，目前當成平均撒在全部 18 種食材上 —— **數量對、種類不對**。影響穿山鼠、石居蟹、萌虻、摔角鷹人等。

**做法**：從上游 `common/src/types/mainskill/mainskills/ingredient-draw-s*.ts` 找出各變體的食材池，加進 `tools/extract-data.mjs` 的萃取範圍，`skillPayload` 改成回傳具體食材向量而非 `ingSpread`。

**注意**：這會改動 `data/game.json` 的結構 → 記得把 `app.js` 的 `SCHEMA` 和 `extract-data.mjs` 的 `schema` 一起 +1（第 3 項做的防護），否則舊快取會配到新資料。

**驗收**：帶穿山鼠的隊伍，食材產出只出現在牠的池子裡，不再是 19 種各一點。

---

## 8. 補上上游落後的資料

上游（Neroli's Lab）本身落後遊戲。踩到才有影響：

| 缺的東西 | 狀態 |
|---|---|
| 脂紅火山（Carmine Volcano）研究區域 | 遊戲有 8 區，資料只有 7 —— 需要三個加成樹果 |
| 活力回復提升 S／M 副技能 | 遊戲有 19 個，資料只有 17 |
| 超夢（2026-09-01 加入） | 種族數值尚未有可靠公開來源 |
| 3 道食譜 | 遊戲約 81 道，資料 78 道 |

**做法**：每次 `npm run data` 前先確認上游是否已補上；還沒補就評估手動加（區域和副技能可行，寶可夢種族數值不要猜）。

**驗收**：頁面「收錄範圍」那行的數字對得上遊戲。目前顯示 `246 隻 · 78 道 · 17 個副技能 · 7 個研究區域`。

---

## 9. 目標函數只有一個純量 —— 這可能是最大的產品層問題

**動機**：工具目前只回答一個問題：「哪 5 隻的**週能量**最高」（或只看樹果／只看料理）。但實際玩的時候，決策常常是**帶條件的**：

- 「我在練某道食譜，這週需要 30 個蜂蜜」→ 要的是滿足食材配額下的最佳隊伍，不是能量最高
- 「我想推圖鑑／刷特定寶可夢的糖果」→ 完全不同的目標
- 「這隻我不想放但想知道差多少」→ 已經有 📌 固定，但沒有「排除後的機會成本」

**這比任何精度改進都可能更影響實際決策。** 引擎的精度誤差是幾個百分點，但「最佳化錯的目標」可以差 20% 以上（`DECISIONS.md` 記著第一版「先選食譜再挑隊伍」就是這樣錯的，放開後多 23%）。

**做法**（由小到大）：
1. **食材配額模式**：讓使用者輸入「本週要湊到 X 個食材 Y」，`scoreTeam` 加一個懲罰項或硬約束
2. **顯示機會成本**：前 8 名旁邊標「不用這隻的話最好是第幾名、差多少 %」
3. 多目標：能量 vs 食材 vs 技能觸發的 Pareto 前緣

建議先做 1，而且**等第 0 項做完再決定** —— 真實使用一週就知道哪個條件才是你真的會設的。

**驗收**：設定「需要 30 個蜂蜜」，推出的隊伍週產蜂蜜 ≥ 30，且在滿足條件的隊伍裡能量最高。

---

## 10. 其他

- **上游更新的自動偵測** —— 現在是「每次 `npm run data` 前先確認上游有沒有補」，靠人記得。加一個 GitHub Actions 排程（每週跑一次 `npm run data`），`git diff` 非空就自動開 PR。這樣「上游落後」那一項就從人工變成推播
- **把這次的對照實驗固化** —— `searchTeams` 已經留了 `finalists` / `prescanLimit` 參數，實驗腳本卻是用完就刪。收成 `npm run verify`（不進 smoke，因為要跑 30 秒以上），讓「捷徑有沒有丟解」變成隨時可重跑的檢查，而不是一次性的
- **`.gitattributes`** —— 目前靠 `core.autocrlf=true` 這個**只存在於這台機器**的 local config 維持行尾一致。版本庫裡存 LF，但換機器 clone 或有人協作，一個 14,032 行的 JSON 被整檔改成 CRLF 提交，diff 就全毀了 —— 而可 diff 正是第 1 項的全部意義。加 `* text=auto eol=lf` 一行就好
- **記住上次所在的 view** —— 重新載入會跳回「推演」，設定 Sheet 時很煩。`localStorage` 已經存了 `psleep-theme` / `psleep-box` / `psleep-sync-*`，加一個 `psleep-view` 即可
- **`npm run serve` 依賴 python3** —— 這台機器有（3.13.14），但換機器就可能沒有。`tests/smoke.mjs` 裡已經有一個 20 行的靜態 server，抽出來共用就沒有外部依賴了
- **`已知簡化` 抽成資料** —— 目前寫死在 `index.html`，應該和引擎能力宣告綁在一起。第 5 項的 `PATHS` 已經解決了「文案提到不存在的檔案」，這一項是它的延伸：讓「引擎有沒有實作某個機制」也由程式宣告，而不是人工維護一段 HTML
- **補能量改成離散事件** —— 目前把每日總量平均攤到白天，對補師略微樂觀（不會撞 150 上限浪費）。要更準就得逐次事件模擬。**第 0 項可能會把這項的優先度往上推**
- **匯出推演結果** —— 存成圖片或文字，方便貼到社群討論
- **專家模式** —— 主／副喜好樹果的頻率修正（`GGEX` ×0.90／×1.15、`CBEX` ×0.80／×1.35 ＋ 攜帶 +5）
